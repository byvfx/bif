# v0.16.0 C4a — Edit Foundation + Doc Sync — Execution Handoff

Date: 2026-04-24
Source plan: `C:\Users\brandon\.claude\plans\could-you-run-the-misty-backus.md`
Supersedes: C4 section of `docs/agent-handoffs/2026-04-22-finish-qt-ui.md` (lines 18, 51, 118–123). C0–C3 remain authoritative in the prior handoff.

## Goal

Land the v0.16.0 layer-aware edit + save **foundation** so user-facing edit features in C4b have a sound substrate. Introduce `EditOperation`/`EditHistory`, add the missing edit-layer FFI surface, fix variant authoring to write to the working layer, and make Ctrl+S actually save the working layer with a dirty bit. Sync project docs to match what was built (vs the aspirational set the audit flagged as doc-only).

## Scope

In scope (four sequential commits + doc sync):

- **C4a-1 — `EditOperation` + `EditHistory` skeleton** in `bif_core::usd::edit_history`. Enum, history (single + group frames), `set_working_layer`, `record`/`undo`/`redo` cursor mechanics. `apply` returns `Err("not wired")` until C4a-4. Unit tests on history mechanics only.
- **C4a-2 — Edit-layer FFI batch.** Six new C++ functions: `usd_bridge_layer_save`, `usd_bridge_layer_permission_to_edit` (read), `usd_bridge_layer_export_as_string`, `usd_bridge_layer_import_from_string`, `usd_bridge_parse_usda`, `usd_bridge_layer_get_attr_value`. Headers, raw externs, safe `UsdStage` wrappers. Direct-FFI save round-trip test + `parse_usda` tests.
- **C4a-3 — Variant edit-context fix.** `cpp/usd_bridge/usd_bridge.cpp:5288` `SetVariantSelection` adds `working_layer_id` param + wraps in `UsdEditContext(stage, layer)`. Read-only check via `PermissionToEdit`. Caller updates in `selection_dispatch.rs:280`. Variant round-trip test.
- **C4a-4 — Wire `EditOperation` end-to-end + `on_save` + doc sync.** `apply` becomes real. `SceneLayerState` extended with `edit_history`. UI dispatch (`selection_dispatch.rs`, `bif_viewport/src/lib.rs`) routes 4 ops through `EditHistory`. `instance_index → SdfPath` boundary helper. `UndoRouter` for combined undo (D1). Dirty bit flips on apply, clears on save. `on_save` impl. ADR-008 authored (ADR-006 already exists for Qt). Doc set updated.

## Files Or Modules

**New Rust:**
- `crates/bif_core/src/usd/edit_history.rs` — `OpinionKey`, `AttrSlot`, `EditOperation`, `EditHistory`, `UndoFrame`
- `crates/bif_core/tests/edit_op_roundtrip.rs` — 5 round-trip tests
- `crates/bif_core/tests/edit_history.rs` — 4 history-mechanic tests
- `wiki/architecture/adr/008-edit-operation-architecture.md` — D1–D5 captured

**Modified Rust:**
- `crates/bif_core/src/usd/mod.rs` — export new module
- `crates/bif_core/src/usd/cpp_bridge.rs:1731,2413,2460` — variant signature, new safe wrappers
- `crates/bif_core/src/usd/ffi_raw.rs:947,1081` — variant extern updated, 6 new externs
- `crates/bif_core/src/scene_layer_state.rs:27,95` — add `edit_history` field, `set_edit_target` method
- `crates/bif_viewport/src/selection_dispatch.rs:186-294` — route through `EditHistory`
- `crates/bif_viewport/src/lib.rs:1563-1789` — `instance_to_opinion_key` helper, `UndoRouter`, `last_action_stack`
- `crates/bif_qt/src/main_window.rs:919-939,1651` — `on_save` impl, working layer sync

**Modified C++:**
- `cpp/usd_bridge/usd_bridge.h` — 6 new function decls + variant signature change
- `cpp/usd_bridge/usd_bridge.cpp:5288` — variant `UsdEditContext` fix + 6 new function bodies

**Documentation (sub-commit C4a-4):**
- `BIF_USD_WORKFLOW.md` — strike: shot templates (line 11), `layer_order` (131,275), `USDA preview` (443-448), future camera/proxy loading Phase 1 (89,95,1152), schema validation (1300-1368). Update §603-720, §1118-1294 to match D1–D5.
- `MILESTONES.md` — v0.16.0 row rescoped to C4a foundation + C4b features
- `ROADMAP_DETAIL.md` — v0.16 detail section: C4a/C4b breakdown
- `SESSION_HANDOFF.md` — current state reflects audit + C4a queued
- `FEATURES.md` — Save row marked in-progress (foundation)
- `docs/agent-handoffs/2026-04-22-finish-qt-ui.md` — C4 section header notes "superseded by 2026-04-24-v0.16-c4a-edit-foundation.md"
- `wiki/_index.md` — link ADR-008
- `CHANGELOG.md` — `[Unreleased]` entries appended per commit
- `devlog/2026-04/DEVLOG_2026-04-24.md` (or session date) — execution log

## Constraints

- **Sub-commit order C4a-1 → C4a-2 → C4a-3 → C4a-4 is mandatory.** C4a-4 depends on all prior. Each must build green and pass tests independently.
- **D1–D5 are LOCKED** (see plan file or ADR-008). Do not relitigate during execution. Parallel undo stacks, dual-track identity, edit-target on `SceneLayerState` (no new `SceneManager` type), composition-arc edits as `EditOperation` variants, soft conflict warn.
- **Do NOT cache `SdfLayer*` in Rust.** Resolve `layer_id → SdfLayer` C++-side per call via existing `find_layer_by_identifier`. Cache invalidation on stage reload/mute is the bug factory.
- **Do NOT split `cpp_bridge.rs` in C4a.** Accept ~250-line bloat; open a v0.17 split tracking issue separately.
- **Do NOT touch `EditState` / `UndoStack`** in `bif_core/src/undo.rs`. Procedural undo stays as-is. New `EditHistory` is parallel.
- **Do NOT cache `SdfLayer*`** (repeated for emphasis). Per-call resolve only.
- **Do NOT delete `usda_preview_text` references in code** — the method does not exist; only doc references need striking.
- **Stage reload discards `EditHistory`.** Documented v0.16 limit. v0.17 can re-resolve by path.
- **`before == None` inverse is a no-op clear placeholder** in v0.16 (defer `usd_bridge_layer_clear_attr` to v0.17).
- **Function before form** — no styling, no design-system work. Graphite stays scheduled at v0.16.5.
- **No `--no-verify`, no `--amend` on pushed commits, no force push to main.** Conventional commit style matches `0322831`, `770e77d`. Terse imperative subject per `CLAUDE.md`.
- **USD env required for bif_core tests** — `. .\setup_usd_env.ps1` + `--test-threads=1`.
- **Qt env required for full workspace build** — `. .\setup_qt_env.ps1`.
- **Target dir:** `C:/Users/brandon/.cargo-target` (desktop dual-machine setup).
- **Commit messages must sacrifice grammar for brevity** per project `CLAUDE.md`.

## Checks To Run

Per sub-commit:

```bash
. .\setup_qt_env.ps1
. .\setup_usd_env.ps1
cargo build
cargo clippy -- -D warnings
cargo fmt --check
cargo test -p bif_math
cargo test -p bif_renderer
cargo test -p bif_viewport
cargo test -p bif_viewer
cargo test -p bif_core -- --test-threads=1
```

After C4a-4 (acceptance gate):

```bash
cargo test -p bif_core --test edit_op_roundtrip --test edit_history -- --test-threads=1
cargo run -p bif_viewer
# manual: open shot.usda → edit transform → Ctrl+S → close → reopen → verify edit persists
usdcat <saved.usda>  # confirm only working layer has new opinions
```

Doc-sync gate (after C4a-4 commits docs):

```bash
rg "usda_preview_text" -g '!*.bak' -g '!devlog/**'           # no matches
rg "shot template model|studio template directory override" -g '*.md'       # only inside "Future" sections
rg "deferred payload enum names" -g '*.md'         # only ADR-005 should name the old enum tokens
test -f wiki/architecture/adr/008-edit-operation-architecture.md
grep -q "ADR-008\|008-edit-operation" wiki/_index.md
grep -qE "C4a|c4a" MILESTONES.md
```

## Acceptance Criteria

**C4a-1 — `EditOperation` skeleton:**
- `bif_core::usd::edit_history` module compiles, exports `EditOperation`, `EditHistory`, `OpinionKey`, `AttrSlot`, `UndoFrame`.
- `EditOperation::apply` returns `Err("not wired")`; `EditHistory::record/undo/redo/begin_group/end_group/set_working_layer` implemented.
- Unit tests in module pass: cursor mechanics, group accumulation, cap enforcement at 256.
- All `cargo test` checks green.

**C4a-2 — FFI batch:**
- 6 new C++ functions implemented in `usd_bridge.cpp` with matching `.h` decls.
- All 6 raw externs in `ffi_raw.rs` and safe wrappers on `UsdStage` in `cpp_bridge.rs`.
- Tests pass: `parse_usda_rejects_garbage`, `parse_usda_accepts_minimal`, direct FFI save round-trip (load → modify via existing FFI → `save_layer` → reopen → modification persists).
- Existing tests stay green (no signature breakage).

**C4a-3 — Variant fix:**
- `usd_bridge_set_variant_selection` takes `working_layer_id`; body uses `UsdEditContext(stage, layer)`.
- `selection_dispatch.rs:280` passes id from `scene_layer_state.edit_history.working_layer_id`.
- Read-only layer surfaces error via status bar prefix `"Save failed: "` or equivalent.
- Test `variant_selection_roundtrips_after_save_reopen` proves opinion lands on working layer (not session/root). Verified via `usdcat` of saved file in test assertion.

**C4a-4 — Wire-up + on_save + doc sync:**
- `EditOperation::apply` wraps FFI write in `UsdEditContext`, flips `LayerInfo.is_dirty`, bumps state revision.
- `SceneLayerState.set_edit_target(idx, &stage)` keeps `edit_history.working_layer_id` in sync; called from `main_window.rs:1651`.
- 4 ops route through `EditHistory`: Transform, Visibility, MaterialAssign, MaterialParamOverride. Plus VariantSelect from C4a-3.
- `instance_index → SdfPath` translation via single helper `instance_to_opinion_key` on viewport.
- `UndoRouter` on viewport pops `last_action_stack: Vec<{Procedural,Usd}>`; Ctrl+Z reverts whichever was most recent.
- `on_save` impl per plan §G — saves working layer only, clears dirty, status bar reports `"Saved <id>"` / `"Save failed: ..."`.
- Window title shows `[*]` when dirty (uses existing `compose_title` at `main_window.rs:946`).
- All round-trip tests green: transform, visibility, material assign, material param override (variant from C4a-3).
- History tests green: dirty cleared after save, undo inverts opinion, group undoes atomically, set_edit_target redirects writes.
- Manual: open shot.usda → edit → Ctrl+S → close → reopen → edit persists. `usdcat` confirms only working layer modified.
- ADR-008 authored, linked from `wiki/_index.md`, cross-referenced from ADR-005.
- Doc-sync gate (above) passes.
- New devlog entry written per `CLAUDE.md` workflow.

## Out Of Scope

- USDA code panel UI, material sheet UI, shading model dropdown — all C4b.
- `on_save_as` — punted to C4b unless trivially included with `usd_bridge_layer_export_to_path` FFI.
- `cpp_bridge.rs` module split — v0.17 (open tracking issue during C4a-4).
- Anonymous SessionLayer for solo/hide/displayColor — v0.17.
- File-watcher save-conflict prompt (external edits during open session) — v0.17.
- Full `usda_preview_text` panel — v0.17 or later.
- Schema validation beyond hard FFI errors — v0.19.
- `usd_bridge_layer_clear_attr` (true "remove opinion" inverse) — v0.17.
- Payload toggle, reference add, sublayer reorder ops — v0.17.
- Point-attribute sparse-vs-full-array edits — v0.17.
- Renderer God-object refactor (~75 fields) — leave alone.
- Multi-layer save on Ctrl+S — working-layer-only this pass.
- Lookdev orb — v0.17.
- Real-time USDA parse on keystroke — N/A in C4a (no USDA panel here).
- Sublayer dirty bubbling — only the edited layer marked dirty; document.
- Touching `EditState`/`UndoStack` in `bif_core/src/undo.rs`.

## Unresolved Questions

- **`on_save_as` placement** — C4a-5 (one extra FFI: `usd_bridge_layer_export_to_path`) or punt to C4b? Recommend punt unless executor finds it trivial during C4a-2.
- **`MaterialParamOverride.ShaderValue` typing** — wrap renderer's value type or define USD-flavored in `bif_core`? Latter avoids `bif_core → bif_renderer` dep but adds adapter. Recommend `bif_core`-side type.
- **Material round-trip test fixture** — does `crates/bif_core/tests/fixtures/openpbr_material.usda` already exist, or generate in `_helpers.rs`? Executor verifies first.
- **Sublayer dirty bubbling** — if working = sublayer of root, mark root dirty too? USD says no; recommend follow USD; document the choice.
- **v0.16.5 Graphite styling pass** — confirm still after C4b, not inserted before. Default: after.
- **ADR-008 timing** — author during C4a-1 (locks decisions early, available as reference during C4a-2/3/4) or C4a-4 (captures actual outcomes). Recommend C4a-1.
- **SessionLayer for solo/hide** — confirm defer to v0.17, or squeeze into C4b? Default: defer.
