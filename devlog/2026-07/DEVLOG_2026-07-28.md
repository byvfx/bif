# Development Log - 2026-07-28

## Session Duration
Several short human-in-the-loop sessions across the week; ~2h active audit time on the Windows box.

## Goals
- Execute the #22 node-graph audit: confirm the dogfood loop (lay nodes → build scene → HDRI light → render) works end-to-end in the running Qt app.

## What I Did
- Built + ran `bif_viewer` (USD + Qt env sourced). Walked the core workflow nodes live, human-in-the-loop.
- **Confirmed the dogfood loop is restored:** `Cube → ScatterPoints (1000 pts) → PointInstancer (1000 instances) → HdriEnvironment (8k RADIANCE HDRI + importance sampling) → IvarRender (16 SPP in 1.34s)`. Headline result for #22.
- Filed 3 issues from confirmed breaks (all milestoned v0.17.0):
  - **#26** — framing broken: `on_frame_selected` (`main_window.rs:1859`) is a stub ("Phase E.2 wires bounds calc"), never calls the working backend (`selection_dispatch.rs:738`, `lib.rs:1556`); no Frame All action exists.
  - **#27** — HDRI decoder rejects valid `#?RGBE` signature (`bif_core/src/hdr.rs:65`, image-crate `HdrDecoder` only accepts `#?RADIANCE`). Fix documented in the issue: `normalize_radiance_signature` helper.
  - **#28** — crash (`STATUS_ACCESS_VIOLATION`) on empty / mis-wired PointInstancer.
- Kept local WIP in `main_window.rs` (`node_graph_add_node` → 3-way result: renderer-not-ready / unsupported / success). Verified live: the "renderer not ready" message fired correctly when nodes were added before the viewport woke.
- Pasted results table into #22; commented corroboration on #28.

## Learnings
- **Backend intact, as the exploration predicted** — the full procedural chain evaluates via auto-cook; the parity gaps are Qt param coverage, not lost functionality.
- **HDRI decoder bug is signature-class-wide**, not file/size-specific: `#?RGBE` fails, `#?RADIANCE` loads (incl. an 8k file). Root cause is the `image` crate only accepting the `#?RADIANCE` literal.
- **PointInstancer crash is specific to the empty-instance path** — correct wiring (Scatter→points, Cube→prototype) produced 1000 instances with no crash. It's a native access violation (no Rust backtrace) → needs a debugger to pin the frame.
- Handoff's HDRI path (`E:\...`) was the *other* machine's drive (dual-machine setup) — wrong on this box; used `Z:\_HDRIs\HDRI_Haven\`.

## Next Session
- Finish the audit: exercise the remaining 5 node types (UsdRead, Xform, UsdExport, UsdPrim, GraftBranches); complete the #22 table; close #22.
- Then scope fix PRs — priority: **#27** HDRI decoder (quick, documented) → **#28** instancer empty-guard → **#26** framing → Scatter / Primitive / Instancer param panels.
- Add headless `bif_viewport` tests for node-graph population (assert on `working_scene`, no GPU needed).

## Blockers / Questions
- #28 native crash needs a debugger (WinDbg/cdb) or crash dump to capture the faulting stack — `RUST_BACKTRACE` won't help on an access violation.
- Decide milestone home for #26/#27/#28 if v0.17.0 gets tight (could move framing to v0.17.5).
