# Development Log - 2026-03-06

## Session Duration
~15 minutes

## Goals
- Fix CI check job failing due to bif_core build.rs panicking without vcpkg

## What I Did
- Added vcpkg existence check in `crates/bif_core/build.rs` — early return with warning when vcpkg not found, allowing clippy to pass without USD env
- Removed `cargo test -p bif_renderer` and `cargo test -p bif_viewport` from CI check job (they fail at link time without USD libs)
- Updated CHANGELOG.md with fix entries

## Learnings
- Clippy doesn't link, so skipping the C++ bridge build in build.rs is sufficient for clippy-only CI
- Tests still need the actual .lib files, so only bif_math (no USD deps) can run in CI

---

## Session 2 Duration
~10 minutes

## Goals
- Fix CI false positive: GH Actions has `C:\vcpkg` dir but no USD packages, causing build.rs to proceed and fail

## What I Did
- `build.rs`: Changed vcpkg check from directory existence → toolchain file + USD header (`pxr/pxr.h`) check. GH Actions has vcpkg toolchain but no USD packages. Resolved path passed to `build_usd_bridge()`
- `vcpkg.json`: Removed `pxr` — not a standard vcpkg port, CI fails trying to install it. USD is built from source locally, not via vcpkg
- Check job: no vcpkg step needed — build.rs toolchain check correctly skips USD bridge
- Updated CHANGELOG, SESSION_HANDOFF

## Learnings
- GH Actions windows-latest has `C:\vcpkg` pre-installed but empty — directory check is insufficient
- Checking the toolchain file is more robust since that's what CMake actually needs
- `pxr` (OpenUSD) is not a standard vcpkg port — can't install via `run-vcpkg` action

## Next Session
- Push and verify CI check job passes on GitHub
- Visual denoise test, VFX code review, Ivar Xform
