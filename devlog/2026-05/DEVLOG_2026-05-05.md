# Development Log - 2026-05-05

## Session Duration
~2.5 hours

## Goals
- Commit the dogfood viewport edit repair on the real `G:` checkout.
- Make visibility toggles safe in the Qt scene browser.
- Refresh viewport, scene browser, and inspector state after USD edits, undo, redo, material edits, visibility edits, shading swaps, and USDA Apply.

## What I Did

### Qt scene-browser visibility
- Moved visibility control into a dedicated fixed-width scene-browser column.
- Kept tree expansion and row selection in the Prim/name column.
- Removed the Property Inspector `Visible` checkbox so the tree eye is the single visibility toggle surface.
- Bumped scene-browser revisions after visibility/material/USDA/shading/undo/redo edits so icon state refreshes with the stage.

### USD edit refresh
- Added `Renderer::reload_after_usd_edit()` as the shared post-edit refresh path.
- Invalidated/requeried USD bridge caches after USDA layer import.
- Refreshed hidden prim paths from the live stage and filtered hidden instances during working-scene rebuild.
- Synced viewport material data from the live USD stage after material edits and undo/redo.
- Marked material data dirty before viewport reloads so the GPU material table is rebuilt.

### Dogfood tracking
- Preserved the dogfood test observations in `BUGLIST.md`.
- Added the execution handoff under `docs/agent-handoffs/`.
- Updated `CHANGELOG.md` and `SESSION_HANDOFF.md` for the current repair state.

## Issues Found
- The earlier shell path was wrong for this work; the real checkout is `G:\__projects\_programming\rust\bif`, mapped through `\\freya\D_rename`.
- The modified `test_assets/scene/root.usda` LFS pointer appears to be dogfood/manual-save dirt and is intentionally not part of the commit.
- The broader material-node redesign is still out of scope; the Property Inspector material sheet remains the validation UI for now.

## Current State
- Automated validation passes on `G:` with Qt/USD environment loaded.
- The scene-browser eye column owns visibility clicks without sharing the name/branch hit area.
- USD edits and undo/redo now converge through the same viewport refresh path.
- Remaining dogfood work is narrower: manual Qt smoke on the same production scene and follow-up design for material binding/editing in the node graph.

## Learnings
- The tree expander must be visually and behaviorally separate from visibility toggles; `QTreeView::setTreePosition()` lets the eye column stay fixed while the hierarchy remains in the Prim column.
- USDA Apply and material edits need cache invalidation plus working-scene reload, not just a stage opinion write.
- The material sheet refresh depends on the same layer-state revision signal as the scene browser; missing revision bumps make correct data look stale.

## Next Session
- Manual Qt smoke: visibility eye, material params, material binding, shading swap, USDA Apply, undo, redo.
- Decide the material binding/node-graph workflow before expanding the material UI.
- Leave `test_assets/scene/root.usda` out unless a specific fixture update is intended.

## Wiki Links
- [[scene-browser|Scene Browser]] — visibility column and tree interaction model.
- [[bif-usd-integration|BIF USD Integration]] — live stage refresh after edit-layer changes.
- [[edit-target|Edit Target]] — layer-directed USD opinion authoring.
- [[material-editor|Material Editor]] — follow-up design area for material binding and parameter workflows.
