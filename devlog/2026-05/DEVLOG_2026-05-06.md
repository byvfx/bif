# Devlog - 2026-05-06

## Session Duration

Interactive implementation and review pass for the Qt node graph dogfood path.

## Goals

- Implement the latest `docs/agent-handoffs` node MVP plan without touching the held Graft Branches design.
- Make Qt graph node creation produce real backend state for supported nodes.
- Remove startup/demo data from empty-stage layer and scene views.
- Log the remaining dogfood issues and run the `/bif-commit` validation flow.

## What I Did

- Wired Qt node graph right-click creation into renderer node creation for supported node types.
- Added status-bar feedback for node add/delete/select actions and held Graft Branches as visual-only.
- Removed demo/template seeding from the Qt graph and scene browser empty state.
- Added node deletion from the Qt graph, including backend snarl-node removal and stale-id guards.
- Mirrored selected node prim paths into the scene browser and guarded external selection sync against re-entry.
- Added authored `UsdPrim` graph-only prims to the scene browser cache so newly added nodes become visible.
- Logged the observed node MVP dogfood follow-ups in `BUGLIST.md`.

## Issues Found

- Qt delete originally removed only the visual node and scene-side data, leaving the backend graph node alive.
- The held Graft Branches path was still computable from the old property inspector path; it is now explicitly inert there.
- Scene-browser external selection sync could re-enter its own user-selection callback.
- The existing viewport snapback behavior remains logged for follow-up investigation.

## Current State

- Graft Branches remains intentionally held for redesign.
- Qt node creation, deletion, status feedback, empty-stage cleanup, and scene-browser mirror paths are implemented for the current dogfood scope.
- Untracked planning/audit artifacts remain outside this commit unless intentionally staged later.

## Learnings

- The Qt graph needs to update both its visual items and the renderer's `egui_snarl` state; cleaning only renderer side maps is not enough.
- Scene-browser cache invalidation is the useful shared path for showing authored graph-only prims immediately after node creation.

## Next Session

- Investigate viewport transform snapback after release.
- Decide the Graft Branches design before enabling backend creation or compute behavior.
- Add connection/wire editing once node execution semantics are settled.

## Wiki Links

- [[BIF Qt UI]]
- [[Node Graph]]
- [[Dogfood Notes]]
