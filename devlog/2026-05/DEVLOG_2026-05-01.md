# Development Log - 2026-05-01

## Session Duration

~1 hour (planning + doc reconciliation)

## Goals

- Audit BIF_USD_WORKFLOW.md vs. shipped v0.16.0 code
- Tag aspirational sections as `_Future_`
- Sync drift into matching wiki notes

## What I Did

- Three Explore agents audited 12 doc claims across edit foundation, node graph, and milestones. Result: 9 exact matches, 2 minor drifts, 4 aspirational sections.
- Updated `BIF_USD_WORKFLOW.md`:
  - Date stamped 2026-05-01 (no version bump per user direction)
  - Status flipped from "C4a in progress" to "v0.16.0 (C4a + C4b) shipped"
  - Operation surface expanded to 7 variants (added `ReplaceLayerContents`, `SetShaderId`)
  - "Live USDA" wording → "Apply-only" everywhere; live preview tagged `_Future_`
  - Undo router language replaced with "per-stack undo, unification deferred"
  - Tagged `_Future_` on: Node Graph blue/orange, Render Context, Eager Eval + Stale Cleanup, Session Layer, Vertex Editing, Animation Overrides, Layer Diffing
  - Implementation Phases: Phase 3 + Phase 4 marked shipped
  - Glossary: added "Apply-only USDA panel", flagged Eager Evaluation as future
- Wiki sync (date-stamped 2026-05-01):
  - `wiki/usd/bif-usd-integration.md` — replaced "v0.14.0+ moves toward layer-aware editing" with shipped status + future list
  - `wiki/usd/layer-aware-stage.md` — summary now covers v0.16 edit/save; events table reflects current behavior
  - `wiki/usd/_index.md` — refreshed layer-aware-stage one-liner; added ADR 008 link
  - `wiki/architecture/node-graph-system.md` — Future Blue/Orange Classification section now tagged `_Future_`
  - `wiki/architecture/adr/008-edit-operation-architecture.md` — status → implemented; D1 router language softened
  - `wiki/concepts/edit-target.md` — "In BIF" updated to v0.16 reality
  - `wiki/_index.md` — date stamp only
- No code changes. CLAUDE.md "10 node types" left alone (count too volatile).

## Key Decisions

- Date stamp instead of version bump (user preference). Workflow doc stays at v0.3.0.
- Drop hardcoded node counts from the workflow doc — count drifts every release.
- Inline `_Future_` tags rather than splitting into a separate "design" doc.

## Learnings

- Explore subagents are very effective for drift audits — three agents in parallel covered the whole doc in one shot.
- The doc was 95% accurate for what shipped; the 5% drift was concentrated in aspirational future-state pseudocode that read as if implemented.

## Next Session

- Move on to v0.16.5 Qt Polish (Graphite styling pass) per MILESTONES.md.
- Or v0.17 prep: viewport perf, `cpp_bridge.rs` split, payload policies.

## Blockers

None.
