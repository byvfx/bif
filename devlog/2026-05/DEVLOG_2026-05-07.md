# Development Log - 2026-05-07

## Session Duration
~15 min

## Goals
- Commit repo housekeeping: remove old codex agent system, replace with `.pi/` pi skills

## What I Did
- Staged deletion of 29 stale files: `agents/` (codex agent configs), `reviews/` (old review artifacts), `scripts/` (install/sync scripts), `debug_output.txt`, `.mcp.json`
- All deleted content moved to `_deprecated/` for historical reference
- Added `.pi/` directory with bif-commit and vfx-reviewer skills + chains config
- Updated CHANGELOG.md with cleanup entries
- Updated SESSION_HANDOFF.md with 2026-05-07 entry
- Committed as "chore: cleanup agents/ → .pi/ + _deprecated/"

## Learnings
- `.pi/` skills system replaces old codex agent workflow — chains defined as markdown
- `_deprecated/` is a safer archive pattern than out-of-repo backups
- Pre-commit checks pass even on file-only changes (no code touched)

## Next Session
- Continue v0.16.5 Graphite styling pass on Qt panels

---

## Session 2 — Visibility toggle + USDA Apply: tests + docs

## Session Duration
~20 min (manual review + commit of pi chain work)

## Goals
- Add regression tests for visibility toggle + USDA Apply undo/redo state
- Update BUGLIST/CHANGELOG/SESSION_HANDOFF docs
- Commit the pi chain output

## What I Did
- Worker (pi chain step 2) added 2 regression tests in `edit_op_roundtrip.rs`:
  - `visibility_toggle_undo_redo_state` — verify live `get_prim_info_by_path().visible` through apply→undo→redo
  - `usda_apply_visibility_undo_state` — verify USDA Apply visibility change + undo restores visibility
- Updated BUGLIST.md: marked C4b-Carry-1 fixed, C4b-2 partial fix, added Fixed entry
- Updated CHANGELOG.md: added regression tests entry under [Unreleased]
- Updated SESSION_HANDOFF.md: current state with known limitation noted
- Reviewer (chain step 4) got stuck at 34 turns / 1M tokens — killed and reviewed manually
- All changes clean on review — no fixes needed

## Learnings
- pi subagent chains need tighter turn/budget limits — reviewer step burned 1M tokens
- Manual takeover of stuck chain step is smooth with existing state files
- `write_layer_visibility` bridge function useful for constructing USDA test data

## Next Session
- Manual Qt smoke test (eye toggle, USDA Apply visibility, undo, redo)
- Then merge to main or cut release
