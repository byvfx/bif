# Development Log - 2026-05-02

## Session Duration
0.8 hours

## Goals
- Repair failing GitHub Actions CI from a clean branch off `main`.
- Keep unrelated `Cargo.lock` churn out of the CI fix branch.

## What I Did
- Created `fix/ci-rust-1-95` from current `origin/main`.
- Replaced manual guarded LOD budget division with `checked_div` in `bif_viewport`.
- Added explicit Embree-gated renderer examples for `simple_render` and `cache_bench`.
- Made renderer's `bif_core` dependency optional/default-on, with local no-default HDRI test support and `bif_core` conversion gates.
- Restored cargo-generated `Cargo.lock` version churn so the branch stays scoped.

## Issues Found
- `cargo build` refreshed workspace package versions in `Cargo.lock`; restored it because the plan explicitly kept lockfile changes out of scope.
- PR CI showed Linux no-default tests were still linking `embree4` / `usd_bridge` from library test artifacts, not just examples.

## Current State
- Local validation passed.
- Branch is ready for commit, push, and CI/CD verification.

## Learnings
- Renderer examples that require optional native paths need explicit `required-features` gates or no-default test jobs still try to link them.
- Newer clippy prefers `checked_div` over manual nonzero division guards.

## Next Session
- Watch the pushed `CI/CD` run for `fix/ci-rust-1-95`.

## Wiki Links
- [[Crate Structure]] - Renderer and viewport crate ownership.
- [[wgpu]] - Viewport culling/rendering context.
