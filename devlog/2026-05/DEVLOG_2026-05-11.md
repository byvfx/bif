# Development Log - 2026-05-11

## Session Duration
~1 hour (follow-up after yesterday's visibility commit)

## Goals
- File residual-risk items from yesterday's review into MILESTONES
- Add stopgaps for the items that can't wait for v0.17/v0.18
- Plan the heavier follow-up work (tests + FFI changes)

## What I Did
- `MILESTONES.md` v0.18.0 — added two bullets: defer GPU upload for invisible prototypes (carry-over from v0.16.2), and `cache_prim_data` thread-safety rework (alongside cpp_bridge.rs split).
- `cpp/usd_bridge/usd_bridge.cpp:529` — added "NOT THREAD-SAFE" doc block above `cache_prim_data` documenting the stage-mutex invariant. Audited `bif_viewport` thread-spawn sites; no BIF-side race exists. The user-observed `BindingsAtPrim` warning is USD-internal parallel composition, not our cache.
- `crates/bif_core/src/usd/loader.rs` — added invisible-prototype memory log at load time so users can see when hidden geometry is consuming CPU memory.
- `CHANGELOG.md` — Changed + Fixed entries for the stopgap batch.
- Planned but deferred to a second commit: visibility time-sample FFI param, ancestor-unhide status-bar surface, three regression tests.

## Key Decisions
- Split the v0.16.2 follow-up plan into two commits. The stopgap (annotations + doc + log) is purely additive; the heavier FFI/UX/test work touches live code paths and warrants its own commit for bisection.
- Skipped runtime `TF_VERIFY` thread-id guard for `cache_prim_data` despite reviewer recommendation. The check requires per-stage thread-id storage in the bridge struct — heavier than the value given the audit found no BIF-side race. Deferred to v0.17 alongside the cpp_bridge.rs split.

## Validation
- `cargo build -p bif_core`, `cargo clippy -p bif_core -- -D warnings`, `cargo fmt --check` clean.

## Next Session
- Time-sample FFI parameter (Risk D from plan).
- Ancestor-unhide status-bar surface (Risk C).
- ~~Three regression tests (Risk E)~~ **(done — see commit below)**

## Additional Work (Risk E — regression tests)
Added 4 tests (the plan called for 3 but split the persisted-invisible case into leaf-only and ancestor-pruning variants — the ancestor variant is the most important one since it specifically exercises `MakeVisible`'s ancestor walk):

- `visibility_persisted_invisible_unhides_on_reopen` — leaf hide → save → reopen → un-hide round-trip.
- `visibility_unhide_defeats_ancestor_pruning` — ancestor hide → reopen → un-hide descendant → assert ancestor was un-hidden too.
- `set_layer_muted_rejects_root_layer` — FFI rejection contract; root layer stays un-muted.
- `payload_root_scene_browser_populates_after_load` — payload-rooted asset populates `all_prims` after `load_payloads`.

All pass under `--test-threads=1`. No regressions in the existing 4-visibility + 3-mute baseline. Test fixtures reuse `LayeredStageFixture` + `state_for_working_layer`; payload test uses an inline temp-dir fixture.

## Blockers/Questions
- None.
