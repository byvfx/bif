# Development Log - 2026-04-26

## Session Duration
~4 hours

## Goals
- Implement `docs/agent-handoffs/2026-04-24-v0.16-c4a-edit-foundation.md`.
- Land the edit/save substrate before C4b UI features.

## What I Did
- Added `bif_core::usd::edit_history` with `EditOperation`, `EditHistory`, grouped undo frames, working-layer tracking, and USD shader value typing.
- Extended `SceneLayerState` with `edit_history`, `set_edit_target`, dirty-bit helpers, and USD undo/redo entry points.
- Expanded the USD bridge with layer save, permission read, export/import text, parse, layer attr read, and working-layer authoring helpers.
- Fixed variant selection to author under `UsdEditContext(stage, working_layer)`.
- Routed viewport transform and variant edits through `EditHistory` while keeping procedural undo parallel through a viewport action router.
- Implemented Qt Ctrl+S working-layer save with `Saved <id>` / `Save failed: ...` status and dirty-bit clearing.
- Added C4a round-trip tests for transform, visibility, material binding, material parameter, and variant selection.
- Synced docs around the C4a/C4b split and added ADR-008 for the edit-operation architecture.

## Issues Found
- The original C4 handoff mixed foundation work with UI features; split C4a from C4b before implementation.
- `BIF_USD_WORKFLOW.md` still described future shot-template and payload-policy ideas as shipped behavior.
- Variant selection was previously authored against the composed stage instead of the selected working layer.

## Current State
- C4a foundation is implemented and validated.
- C4b can start on the editable USDA panel, material sheet, and shading-model dropdown.

## Learnings
- `UsdEditLayer` export-stage helpers were not enough for live stage authoring; C4a needed stage-layer write calls that resolve `SdfLayer` by identifier per call.
- The repo already had ADR-006/ADR-007, so the edit architecture is ADR-008 rather than duplicating ADR-006.

## Wiki Links
- [[architecture/adr/008-edit-operation-architecture]] — write-side edit/save architecture for C4a.
- [[architecture/adr/005-layer-aware-read-model]] — read-side model extended by C4a write support.

## Next Session
- Run full validation sweep if not already done: clippy, fmt check, crate tests, full `bif_core`.
- Start C4b on top of C4a: editable USDA layer panel, material sheet, shading-model dropdown.
