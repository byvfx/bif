# Development Log - 2026-04-08

## Session Duration

~2 hours (planning + implementation)

## Goals

- Plan remaining v0.13.0 items (5 total) for final ship commit
- Start implementing quick-win items

## What I Did

### v0.13.0 Ship Plan

Created comprehensive plan for 5 remaining items (20-33h est):

1. **Dome Light Bug** (2-4h) — Houdini `DomeLight_1` not detected
2. **Linux Feature-Gate + CI** (4-6h) — Embree behind feature flag
3. **MaterialX Displacement** (4-8h) — native `ND_displacement_float/vector3` extraction
4. **SceneQuery Migration** (2-3h) — `scene_browser.rs` → `&dyn SceneQuery`
5. **FaceVarying UVs + Subdiv Displacement** (8-12h) — fix broken subdiv UVs, Embree faceVarying topology, CPU displacement for subdiv

### Item 1: Dome Light Bug — DONE

Root cause: Houdini 20+ exports `DomeLight_1` (new USD Lux schema), C++ bridge only checked `IsA<UsdLuxDomeLight>()`. Fix:

- Added `#include <pxr/usd/usdLux/domeLight_1.h>`
- Added `|| prim.IsA<UsdLuxDomeLight_1>()` to detection
- Flipped attribute lookup: try `inputs:texture:file` first (new schema), fall back to `texture:file`
- Confirmed: HDRI loads correctly from `displacement_test.usda`

### Item 2: Linux Feature-Gate — DONE

- Added `embree = []` feature to `bif_renderer/Cargo.toml` (default on)
- Gated `embree`, `embree_ffi`, `pick_scene` modules + re-exports behind `#[cfg(feature = "embree")]`
- `cargo check -p bif_renderer --no-default-features` passes (no Embree linking)
- Expanded Linux CI: clippy + test `bif_renderer --no-default-features` (97 tests pass, 6 pre-existing HDRI failures unrelated)

## Key Finding

**Subdiv UVs are completely broken** — C++ bridge vertex-splits faceVarying UVs (modifying topology), but Embree subdiv path uses `vertices_orig` + original polygon topology. UV count mismatches position count → UVs skipped entirely. Fix requires: preserving raw faceVarying UV data in C++ bridge, adding Embree faceVarying topology FFI (`rtcSetGeometryTopologyCount`, `rtcSetGeometryVertexAttributeTopology`), and setting up dual topology in subdiv geometry.

## Learnings

- USD `DomeLight_1` is a distinct prim type in USD 25.11 — `IsA<UsdLuxDomeLight>()` does NOT match it. Separate `domeLight_1.h` header + `UsdLuxDomeLight_1` class required.
- Embree 4 supports faceVarying attributes via separate topology slots — topology 0 = vertex connectivity, topology 1+ = faceVarying index buffers.
- Feature-gating Embree was clean — 3 `#[cfg]` on module declarations + 2 on re-exports. No stub types needed since consumers (bif_viewport) always use default features.

## Next Session

- Item 3: Native MaterialX displacement in C++ bridge
- Item 4: SceneQuery `scene_browser.rs` migration
- Item 5: FaceVarying UVs for subdiv (C++ bridge + Rust FFI + Embree topology)
- After all 5: version bump → v0.13.0 ship commit

## Wiki Links

- [[USD Light Schema]] — DomeLight vs DomeLight_1 schema differences
- [[Embree Integration]] — feature gating, subdivision surface support
- [[Subdivision Surfaces]] — faceVarying UV issue root cause
- [[C++ Bridge Architecture]] — dome light detection, MaterialX displacement gap
