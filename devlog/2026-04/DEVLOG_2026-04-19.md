# Development Log - 2026-04-19

## Session Duration
~30 min (commit + planning)

## Goals
- Commit Tier 1.5 per-attribute opinion color (was implemented but uncommitted)
- Plan camera-picker feature

## What I Did
- Committed Tier 1.5 (`43c1357`): per-attribute opinion dot in property inspector. Each row now calls `selected_prim_attr_color_index_at(i)` which routes through `get_attribute_opinions(prim, attr)` to find the winning layer per attribute — previously every row showed the same prim-level winning color.
- Planned camera-picker feature: UsdGeomCamera dropdown + 6 ortho presets + free-fly toggle. Key finding: `CameraSource` enum, `OrthoPreset`, and `sync_viewport_to_usd_camera()` all already exist — mostly wiring work.

## Learnings
- Tier 1.5 had been implemented last session but not committed (session ended before the commit step)
- The camera system is more complete than expected: `sync_viewport_to_usd_camera`, `OrthoPreset`, `ProjectionMode::Orthographic` all ready to use

## Next Session
- Implement camera-picker (4 files: cpp_bridge.rs, bif_viewport/lib.rs, main_window.rs, window_builder.cpp)
- See plan at `.claude/plans/lets-pick-up-where-shimmering-gadget.md`

## Wiki Links
- [[2026-04-19-tier1-5-commit]] — session journal
