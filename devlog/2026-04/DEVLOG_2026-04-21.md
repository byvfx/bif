# Development Log - 2026-04-21

## Session Duration
~30 min

## Goals
- Option 2 polish batch (pre-merge hygiene) from the consolidated review plan
- Figure out why the site isn't getting updated

## What I Did
- **About string.** `on_about` hardcoded `v0.14.0 (Phase B shell)` — swapped for `crate::BIF_QT_VERSION` const (reads `CARGO_PKG_VERSION`).
- **env_logger dedup.** `bif_viewer/src/main.rs` called bare `init()` before `bif_qt::run()`'s tuned `try_init()`. Bare call won and silently dropped the `wgpu_hal=error` filter — `d290c9d`'s log-noise fix never applied to the real binary. Deleted the bare call + dep from `bif_viewer/Cargo.toml`. Comment at the call site documents the trap.
- **`bif_qt_spike` deleted.** Workspace member removed from `Cargo.toml`, `rm -rf crates/bif_qt_spike`, doc comment in `setup_qt_env.ps1` updated. Git history retains the spike.
- **Orphan `on_open_stage` deleted.** Zero C++ call sites (live path is `on_stage_path_opened`). Removing the declaration + impl prevents a future wiring accident.
- **Site regen.** `scripts/generate-site.sh` pulls devlog + USD docs + CHANGELOG into `site/src/`. Touched `SUMMARY.md` + `reference/changelog.md` this pass.
- **Site deploy root cause found:** `.github/workflows/pages.yml` only fires on `push: branches: [main]`. `v0.15-qt` has been pushed but never triggered a Pages build. Site will update the moment we merge to main.

## Learnings
- The pages workflow's `paths:` filter is comprehensive (devlog, docs, site, CHANGELOG, MILESTONES, etc.), but the branch filter gates it to `main`. Any work on feature branches shows nothing until merge. Worth documenting if other branches start wanting preview deploys (use `workflow_dispatch` or a staging environment).
- `on_open_stage` being orphaned is exactly the "shadow stub" smell the general reviewer flagged — the declaration was in the extern block, invokable from Qt, but nothing actually called it. Easy to delete because nothing pointed at it; would have been gnarlier if the menu action had rebound to it vs the live path.

## Next Session
- Merge `v0.15-qt` → `main` with `--no-ff` (preserves phase-by-phase commit history for the migration).
- After merge: verify the Pages deploy fires, visit the published URL, confirm the v0.15 work lands in the changelog + devlog sections.
- Remaining review items (Commits 3–5 in the consolidated plan): property inspector O(N²) cache, ortho aspect + QTimer gating + demo-seed guard, optional scene browser lazy `fetchMore`. All fine to pick up on `main` directly post-merge.
