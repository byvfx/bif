# Development Log - 2026-04-04

## Session Duration

~2 hours

## Goals

- Fix code review issues from VFX reviewer
- Add display color (primvars:displayColor) support
- Add shading mode toggle (Textured vs Display Color)

## What I Did

- **Code review fixes** — 4 critical issues from VFX code reviewer:
  1. Subdiv material lookup: `0usize.min(x)` always 0, now uses `triangle_material_ids[prim_id]`
  2. Subdiv back-face bitangent flip (matches triangle path)
  3. Extracted `safe_normal_matrix()` helper with determinant guard, used in all 3 sites including `update_transforms`
  4. Corrected Drop impl safety comment
- **Display color support** — `primvars:displayColor` from C++ bridge flows through `Mesh.display_color` to `Vertex.color`. Viewport shader passes vertex color through `VertexOutput` and uses it as base color when no texture is bound.
- **Shading mode toggle** — `ShadingMode` enum (Textured/DisplayColor) in `DisplaySettings`. GPU uniform `shading_mode` in `CameraUniform`. Shader branches: Display Color mode forces vertex color ignoring textures. UI dropdown in Display settings panel.
- **Markdownlint fixes** — `.kilo/` added to gitignore + pre-commit ignore. Fixed bare code fences in `docs/usd/llm-reference.md`.

## Issues Found

- `.kilo/` config files were accidentally committed and broke markdownlint pre-commit hook
- ALab scene doesn't have displayColor (uses full materials), so Display Color mode shows grey — expected

- **Displacement texture pipeline foundation** — `displacement` input + scale extracted from UsdPreviewSurface in C++ bridge, flows through FFI → `UsdMaterialData` → `Material`. Ready for CPU vertex displacement.

## Next Session

- CPU vertex displacement (load texture, sample per-vertex UV, displace along normal)
- Phase 3: Variant set UI, curves in Ivar
- Phase 4: OpenVDB volume rendering

---

## Session 2

## Session Duration

~1 hour

## Goals

- UI/UX review of Qt design spec using ui-ux-pro-max skill
- Identify gaps before v0.15.0 implementation

## What I Did

- **ui-ux-pro-max review** — ran full skill review against `docs/ux/UI_DESIGN.md`; existing design rated strong, identified 9 gaps
- **Qt UI spec §17-25** — added context menus, multi-select, undo feedback, long-op progress tiers, reduced-motion accessibility, scene tree filter, error state badges, workspace layout storage (global + per-project TOML), cxx-qt binding decision
- **Spec corrections** — click targets raised 24px→32px for rows / 44px for toolbar; node labels 12px (was 10px — too small per mockup feedback)
- **Wireframe selection overlay** — `POLYGON_MODE_LINE` pipeline + `VariantChanged`/`FrameSelected` events committed (was unstaged)
- **cxx-qt decided** — binding strategy locked; drag-and-drop deferred to v0.16.0; Wacom TODO deferred post-v0.15.0

## Learnings

- The skill's web-oriented design system didn't map well to DCC (no Qt/desktop-app templates), but UX principles (touch targets, reduced-motion, error states) are universal
- 24px click targets feel fine with a mouse but are genuinely painful on Wacom — worth fixing before Qt build starts

## Next Session

- CPU vertex displacement
- Phase 3: Variant set UI
