# Development Log - 2026-04-27

## Session Duration
~2 hours

## Goals
- Make C4a dogfood usable by adding a Qt viewport path for moving selected prims.
- Verify transform edits author to the working layer and save cleanly.

## What I Did
- Added a wgpu line-overlay translate gizmo for selected viewport instances.
- Routed Qt mouse hover, LMB axis press, drag preview, and release into the viewport renderer.
- Reused the C4a `TransformEdit` path so release records undo and authors to the active USD working layer when a stage is loaded.
- Preserved existing USD xform op types when saving movement, so `xformOp:translate` receives a vec3 instead of a matrix.
- Fixed the outline shader uniform padding that made wgpu reject the selected-outline pass.
- Kept Alt+LMB orbit, MMB pan, wheel zoom, and LMB selection behavior intact.

## Issues Found
- The old transform controls were still egui-only, so Qt had backend edit support but no artist-facing way to move prims.
- Synthetic `/BIF/.../<instance>` paths needed display-path normalization before tree or viewport selections could resolve gizmo instances.
- USD rejects matrix values on translate ops; the bridge now extracts translation vectors for translate-backed prims.
- The Qt relationships tab is still a placeholder and should be restored before broader C4b dogfood.

## Current State
- Select a prim in the viewport or scene tree, click-drag a colored axis handle, then `Ctrl+S` to save the working layer.
- Dogfood confirmed the gizmo appears, moves prims, and no longer trips the USD translate type mismatch.
- Relationship editing/inspection is still not implemented in Qt.

## Learnings
- Keep transform authoring in `bif_viewport::Renderer`; Qt should only forward physical-pixel mouse events.
- `SceneInstances::prim_paths` can be synthetic loader paths, so selection UI paths need normalization at the renderer boundary.
- USD xform edits must preserve the authored op type instead of blindly reusing the first op with a matrix.

## Wiki Links
- [[architecture/adr/008-edit-operation-architecture]] — transform release now flows through the C4a edit history path.
- [[usd/stage-layer-prim|Stage, Layer, Prim]] — working-layer saves depend on authoring opinions to the selected layer only.

## C4b-Carry-2
- Added `EditOperation::ReplaceLayerContents { key, before, after }` plus an `EditOperation::replace_layer` constructor and `AttrSlot::LayerContents` variant.
- Apply path imports `after` via `UsdStage::import_layer_from_string`; inverse re-imports the captured `before`.
- Round-trip test seeds a visibility opinion as `after`, resets, replays via `apply_edit_operation`, then undoes back to `before`.
- Unblocks C4b-2 USDA panel Apply path (next sub-commit).

## C4b-Carry-1
- New `Renderer::dispatch_visibility(prim_path, after)` in `selection_dispatch.rs` reads computed visibility as `before` via `get_prim_info_by_path`, builds `EditOperation::Visibility`, and routes through `apply_usd_edit`.
- New `BifShellState::on_set_visibility(path, visible)` qinvokable forwards to the renderer dispatcher and refreshes status / undo-redo qprops on success.
- Property Inspector `cpp/property_inspector_widget.cpp` gained a `Visible` checkbox in the header; reflects `prim_is_visible_at` on selection change, blocked-signals update, dispatches `on_set_visibility` on toggle.
- Round-trip test `visibility_roundtrips_via_dispatcher` mirrors the dispatcher's surface (read `before`, build op, apply, verify `invisible` lands, undo back to `inherited`).

## C4b-1
- New FFI `usd_bridge_prim_get_bound_material_inputs(stage, prim, out_text, out_shader)` enumerates the bound material's surface shader inputs as `name\ttype\tvalue\n` records using a function-local `thread_local` buffer per the C4a contract.
- Safe wrapper `UsdStage::get_bound_material_inputs` returns `(shader_path, Vec<BoundMaterialInput>)`.
- New `Renderer::dispatch_material_param_override` and `dispatch_material_assign` mirror the visibility dispatcher.
- New qinvokables on `BifShellState`: `selected_prim_material_input_count/name_at/type_at/value_at`, `selected_prim_material_shader_path`, `on_set_material_param(name, type, value)`, `on_bind_material(material_path)`.
- Cache field `selected_material_inputs_cache` refreshes on selection / layer-revision bumps.
- Material Sheet tab in `cpp/property_inspector_widget.cpp` groups inputs by OpenPBR / UsdPreviewSurface section (Base, Specular, Transmission, Subsurface, Coat, Emission, Geometry, Other). Editors per type: `QDoubleSpinBox`, `QSpinBox`, `QCheckBox`, color swatch + `QColorDialog` (sRGB→linear at boundary), `QLineEdit` fallback.
- `Bind…` header button surfaces a `QInputDialog` for the material path.
- Round-trip test `bound_material_inputs_returns_shader_inputs` covers the new FFI.

## C4b-2
- New `Renderer::dispatch_replace_layer_contents(layer_id, new_text)` validates via `parse_usda`, captures `before` via `export_layer_as_string`, then applies a `ReplaceLayerContents` op through the C4a edit history.
- New qinvokables `active_edit_target_layer_text` (export current edit target as USDA) and `on_apply_usda(text) -> error_msg` (returns empty on success, parse/dispatch error on failure).
- New `cpp/usda_panel_widget.{h,cpp}` widget hosts a monospace `QPlainTextEdit`, Apply button, and red status label. Reload-on-revision-bump suppressed while editor has focus so external undo/redo doesn't clobber unsaved keystrokes.
- `window_builder.cpp` adds `View → USDA Source` checkable menu action, `dock_usda_source` `QDockWidget` tabified with the Node Graph dock at the bottom, hidden by default. Command palette entry "View: Toggle USDA Source" added.
- `build.rs` registers the new C++ files for cc + moc.

## C4b-3
- New FFI `usd_bridge_layer_set_shader_id` (authors `info:id` token on a surface shader) and `usd_bridge_prim_get_bound_shader_id` (reads it).
- Safe wrappers `UsdStage::set_layer_shader_id` / `get_bound_shader_id`.
- New `EditOperation::SetShaderId { key, before, after }` variant + `AttrSlot::ShaderId` slot. `apply` / `inverse` round-trip the id token through the working layer.
- New `Renderer::dispatch_swap_shading_model(prim_path, target_model)` wraps the id swap + best-effort param remap (`base_color` ↔ `diffuseColor`, `specular_roughness` ↔ `roughness`, `base_metalness` ↔ `metallic`, `specular_ior` ↔ `ior`, `emission_color` ↔ `emissiveColor`) in `begin_group` / `end_group`. Returns the dropped param list for the lossy warning.
- Material Sheet header gains a `QComboBox` (OpenPBR / UsdPreviewSurface). Selection change pops a `QMessageBox` warning, calls `on_set_shading_model`, then surfaces dropped params via a follow-up info dialog.
- New qinvokables `selected_prim_material_shader_id` / `on_set_shading_model`.
- Tests: `set_shader_id_roundtrips` (apply → undo restoring `info:id`) and `shading_model_swap_undoes_atomically` (group of two records collapses into one undo frame).

## C4b-4 (doc sync, this commit)
- `CHANGELOG.md` `[Unreleased]` lists the five C4b sub-commits.
- `MILESTONES.md` moves v0.16.0 to the shipped/archive section, promotes v0.16.5 (Graphite) to "In Progress", and details the C4b feature surface + deferred items.
- `FEATURES.md` Save row is now "shipped" with sub-commit references.
- `SESSION_HANDOFF.md` lead entry is now `C4b editor features shipped` with a per-sub-commit summary, validation note, and a v0.17 hand-off list.
- Doc-sync gate (`rg "C4b" MILESTONES.md SESSION_HANDOFF.md FEATURES.md`) shows hits in all three; `rg "in.progress" FEATURES.md` is empty.
- Manual `usdview` reopen of a Ctrl+S working layer is the recommended next human-in-the-loop sanity check; not run from this autonomous session.

## Final v0.16.0 Status
- Six sub-commits shipped: `4dc7392` (Carry-2), `53b8b53` (Carry-1), `ca31587` (C4b-1), `8455b8e` (C4b-2), `622db71` (C4b-3), and this doc-sync commit (C4b-4).
- `cargo test -p bif_core --test edit_op_roundtrip --test edit_history -- --test-threads=1` green at 10 tests.
- `cargo clippy --workspace -- -D warnings` and `cargo fmt --check` clean.
- v0.16.0 milestone closed end-to-end: edit any of (transform, visibility, variant, material binding, material parameter, shading model swap, wholesale layer USDA) → Ctrl+S → reopen.

## Next Session
- v0.16.5 Graphite styling pass.
- v0.17.0: split `cpp_bridge.rs`, payload policies, file-watcher save-conflict prompt, line/col USDA parse errors, real-time USDA parse, drag-drop material binding, lookdev orb, `usd_bridge_layer_clear_attr`.
