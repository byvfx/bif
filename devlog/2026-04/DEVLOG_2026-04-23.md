# Development Log - 2026-04-23

## Session Duration
~1.5 hr

## Goals
- Validate and land C1 from `docs/agent-handoffs/2026-04-22-finish-qt-ui.md`
- Keep the commit scoped to the foundations tranche before moving to C2

## What I Did
- Re-validated the in-progress `finish-qt-ui` C1 diff against the handoff before committing it.
- Wired Qt Edit menu Undo/Redo through `BifShellState` qproperties/invokables so action enable state tracks the live renderer undo stack.
- Replaced the writable-layer heuristic with real USD `SdfLayer::PermissionToEdit()` carried through the C++ bridge, Rust FFI, and layer-stack structs.
- Added a test-only bridge helper to flip layer `PermissionToEdit` on the multilayer fixture so the new editability path is regression-testable.
- Hid the node graph dock by default behind a persisted experimental toggle instead of showing it in the default dock layout.
- Updated release-tracking docs for the C1 tranche:
  - `CHANGELOG.md`
  - `SESSION_HANDOFF.md`
- Ran validation:
  - `cargo fmt --check`
  - `. .\setup_qt_env.ps1; cargo build`
  - `. .\setup_qt_env.ps1; cargo clippy -- -D warnings`
  - `. .\setup_qt_env.ps1; cargo test -p bif_math`
  - `. .\setup_qt_env.ps1; . .\setup_usd_env.ps1; cargo test -p bif_renderer`
  - `. .\setup_qt_env.ps1; . .\setup_usd_env.ps1; cargo test -p bif_viewport`
  - `. .\setup_qt_env.ps1; . .\setup_usd_env.ps1; cargo test -p bif_viewer`
  - `. .\setup_qt_env.ps1; . .\setup_usd_env.ps1; cargo test -p bif_qt pick_strongest_writable_sublayer_skips_locked_layers`
  - `. .\setup_usd_env.ps1; cargo test -p bif_core -- --test-threads=1`

## Issues Found
- The active UNC checkout cannot be used as the sandbox working directory directly; repo access in this session had to go through escalated shell commands.
- The targeted `bif_qt` regression test needs both Qt and USD DLL paths in the shell. Qt env alone fails with `0xc0000135` (`STATUS_DLL_NOT_FOUND`).
- The full `bif_core` suite still emits the known noisy USD secondary-thread diagnostics after success; they did not fail the suite here.

## Current State
- C1 foundations are validated and ready to commit on `finish-qt-ui`.
- The branch is positioned to continue straight into C2 after the C1 commit lands.

## Learnings
- Directly toggling `PermissionToEdit()` in the USD test fixture is more reliable than trying to infer non-editability from filesystem readonly flags on this machine.
- Undo/redo enable-state sync can ride the existing viewport frame pump without adding a new shell-to-renderer notification path.
- The node graph preview gate is cleaner as a runtime QSettings toggle than a build-flag split.

## Next Session
- Start C2 quick wins after the C1 foundations commit.
- Wire outline width/color, drag-and-drop stage open, opinion-stack hover tooltip, and the Ivar render trigger/status path.

## Wiki Links
- [[007-shell-state-to-viewport-bridge]] — the Qt undo/redo wiring still rides the existing shell-to-viewport bridge
- [[003-hybrid-usd-workflow]] — edit-target layer selection stays grounded in real USD layer authoring semantics
- [[design-philosophy]] — function-first v0.16 work continues while Graphite stays deferred to `v0.16.5`

---

## Session 2 — C2 quick wins

### Duration
~2.5 hr

### Goals
- Land the C2 quick-wins tranche from `docs/agent-handoffs/2026-04-22-finish-qt-ui.md`
- Keep the branch moving at the documented commit boundary before starting C3

### What I Did
- Finished the Rust shell-side C2 surface in `BifShellState`: outline width/color qproperties, Ivar status polling, a rich attribute-tooltip formatter, and the missing stage open/close status refresh hooks.
- Replaced the hard-coded outline shader constants with a dedicated `OutlineParams` uniform and wired the renderer to update it from `DisplaySettings`.
- Reworked `RenderSettingsWidget` to bind outline controls and Ivar preview state to the live shell instead of keeping those values local-only.
- Added a Render menu `Ivar Render` action, a status-bar Ivar progress chip, and drag-and-drop stage open routing in `window_builder.cpp`.
- Updated Property Inspector attribute rows to preserve raw USD names in the tooltip and show the full opinion stack as rich HTML.
- Corrected the default linear outline color constant to match the actual sRGB `#FFA600` conversion used by the Qt color-picker path.
- Updated release-tracking docs for the C2 tranche:
  - `CHANGELOG.md`
  - `SESSION_HANDOFF.md`
- Ran validation:
  - `. .\setup_qt_env.ps1; cargo fmt --check`
  - `. .\setup_qt_env.ps1; cargo build`
  - `. .\setup_qt_env.ps1; cargo clippy -- -D warnings`
  - `. .\setup_qt_env.ps1; cargo test -p bif_math`
  - `. .\setup_qt_env.ps1; . .\setup_usd_env.ps1; cargo test -p bif_renderer`
  - `. .\setup_qt_env.ps1; . .\setup_usd_env.ps1; cargo test -p bif_viewport`
  - `. .\setup_qt_env.ps1; . .\setup_usd_env.ps1; cargo test -p bif_viewer`
  - `. .\setup_qt_env.ps1; . .\setup_usd_env.ps1; cargo test -p bif_qt`
  - `. .\setup_usd_env.ps1; cargo test -p bif_core -- --test-threads=1`

### Issues Found
- The first `cargo build` pass caught a real missed branch-local enum case: `BuildStatus::Failed` needed to be handled by the new Ivar status formatter.
- The new outline-color regression test exposed that the renderer default linear green channel did not actually match the sRGB `#FFA600` conversion; the default constant was corrected to keep Qt and renderer defaults aligned.
- The full `bif_core` suite still emits the known noisy USD secondary-thread diagnostics after success; they did not fail the suite here.

### Current State
- C2 quick wins are validated and ready to commit on `finish-qt-ui`.
- The next clean boundary is C3 navigation.

### Learnings
- The outline color-space boundary really does belong at the Qt shell edge: QColor/UI values stay sRGB, renderer state stays linear.
- The existing viewport frame pump is sufficient for both undo/redo availability sync and Ivar progress/status sync; no new renderer notification channel was needed.
- App-level drag/drop handling is safest as an event filter plus `acceptDrops` on the main Qt shell surfaces, so drops still work over nested widgets.

### Next Session
- Start C3 navigation: Look Through action surfacing, orthographic toggle/menu affordance, Review workspace, and payload-policy-per-workspace handling.

### Wiki Links
- [[007-shell-state-to-viewport-bridge]] — C2 kept using the existing shell/frame-pump bridge for outline and Ivar status sync
- [[003-hybrid-usd-workflow]] — property-row opinion tooltips expose the same layer-aware composition model the editor is built around
- [[design-philosophy]] — C2 stayed function-first and left Graphite polish out of the critical path
