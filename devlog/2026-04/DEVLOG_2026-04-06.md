# Development Log - 2026-04-06

## Session Duration

~2.5 hours (two sessions)

## Goals

- Remove welcome/open-USD-file overlay from viewport
- Brainstorm BIF's evolving identity as it becomes more USD-native
- Migrate repo from D: to G: (failing drive)

## What I Did

### Session 1: Welcome Overlay Removal

- Removed the centered welcome `egui::Area` overlay that appeared on empty scenes (`render.rs:873-901`)
- The overlay showed "BIF / VFX Scene Assembly" heading + "Open USD File..." button + hint text
- File menu "Open USD..." remains functional — no lost functionality

### Session 2: Identity Brainstorm + Repo Migration

**Identity:**

- BIF reframed as **"USD Orchestration Tool"** — not editor, not assembler
- Scope fence: arrange, compose, override, instance. Not: model, rig, animate, simulate
- First audience: solo VFX artists / small studios (Katana alternative)
- Business model direction: tyFlow-style free beta → low-cost paid
- Hydra delegate worth pursuing post-v1.0
- **Verdict: existing roadmap already aligned, no pivot needed**

**Architecture gap audit:**

- P0: SetEditTarget/GetEditTarget/GetPrimStack FFI (v0.14)
- P1: Stage invalidation, EditOperation, EditHistory (v0.16)
- P2: Renderer god object decoupling (incremental)

**Repo migration D: → G::**

- D: drive failing — migrated to `G:\__projects\_programming\rust\bif`
- Updated vcpkg/OIDN paths in build.rs, setup_usd_env.ps1, CLAUDE.md
- Fixed missing `.vcpkg-root` marker on G: vcpkg
- Verified clean build from G: (5m07s)

**Doc updates:**

- README, BIF_USD_WORKFLOW, SESSION_HANDOFF, CLAUDE.md, CHANGELOG — "USD Orchestration Tool" framing

## Learnings

- "USD Orchestration" captures the identity better than "editor" or "assembler"
- tyFlow's free-beta → paid model is a strong reference for solo-dev VFX tools
- robocopy exit code 1 = success (files copied), not failure
- vcpkg needs `.vcpkg-root` marker file even if all packages are present

## Next Session

- Push to GitHub from G: drive
- Start v0.14 FFI expansion (SetEditTarget, GetPrimStack)
- Consider wiki article on identity/positioning
