# Development Log - 2026-04-22

## Session Duration
~2.5 hr

## Goals
- Move shared agent behavior into repo-owned canonical docs
- Thin the Claude/Kilo wrappers
- Add a practical Codex path for repo-owned `bif-commit`
- Validate the new flow end to end

## What I Did
- Added the shared `agents/` source tree:
  - `agents/base/PROJECT.md`
  - `agents/base/HANDOFF_CONTRACT.md`
  - shared roles and workflows
- Added repo conventions for:
  - `docs/agent-plans/`
  - `docs/agent-handoffs/`
- Slimmed `.claude/` and `.kilo/` workflow and agent files into thin adapters that point back to canonical docs under `agents/`.
- Added `scripts/sync-agent-config.ps1` to regenerate wrappers and repo-local Codex artifacts.
- Added a Claude `/save-plan` command so completed plans can be written into the repo instead of living only under the user-folder Claude plan path.
- Added a repo-generated Codex `bif-commit` skill artifact under `agents/generated/codex/bif-commit/SKILL.md`.
- Added `scripts/install-codex-skills.ps1` to copy repo-generated Codex skills into `~/.codex/skills`.
- Added user-facing docs for:
  - day-to-day shared-agent usage
  - Claude planning → Codex execution handoff
  - step-by-step Codex `bif-commit` setup and usage
- Ran validation:
  - `scripts/sync-agent-config.ps1 -All -WhatIf`
  - `scripts/sync-agent-config.ps1 -All`
  - `scripts/sync-agent-config.ps1 -Codex -WhatIf`
  - `. .\setup_qt_env.ps1; cargo build`
  - `. .\setup_qt_env.ps1; cargo clippy -- -D warnings`
  - `cargo fmt --check`

## Issues Found
- `cargo build` and `cargo clippy` fail immediately in a plain shell because `bif_qt` cannot find Qt. Not a branch bug — just expected environment setup. Loading `setup_qt_env.ps1` fixed both checks.
- Codex does not auto-inject repo-owned docs at session start. The practical fix is explicit bootstrap plus a repo-generated skill/install path, not pretending startup injection exists when it does not.
- Unrelated artifacts are sitting in the worktree:
  - `assets/screenshots/bif_ui_15.png`
  - `assets/screenshots/bif_ui_15_timeline.png`
  - `review/`
  Kept out of scope for this branch/commit.

## Current State
- Shared agent behavior is now repo-owned under `agents/`.
- Claude and Kilo wrappers are thin and syncable.
- Codex has a repo-generated `bif-commit` artifact and install path.
- The branch is ready to commit once the upkeep files are staged with the intended scope.

## Learnings
- The right split is:
  - `agents/` = canonical source
  - `scripts/sync-agent-config.ps1` = wrapper/artifact projection
  - `scripts/install-codex-skills.ps1` = Codex-local install step
- Thin-wrapper architecture works as long as the bootstrap gap is documented honestly. Claude/Kilo can follow the pointer pattern directly; Codex needs either explicit bootstrap or an installed repo-generated skill.
- For Codex specifically, `sync` is not enough. The real chain is:
  - edit canonical doc
  - sync generated artifact
  - install generated artifact
  - use skill

## Next Session
- Commit `agent-config-unification`.
- Merge `agent-config-unification` → `main`.
- From a clean checkout, run `pwsh -File scripts/install-codex-skills.ps1` if the repo-owned Codex skills should become the local default.
- Decide later whether more Codex workflows beyond `bif-commit` actually justify generated/installable skills.

## Wiki Links
- [[2026-04-22-shared-agent-config]] — why the shared `agents/` layer, thin-wrapper projection, and Codex install path are split the way they are
- [[007-shell-state-to-viewport-bridge]] — example of a repo-owned architecture decision that should stay in wiki/docs, not in agent wrappers

---

## Session Duration
~0.5 hr

## Goals
- Execute C0 from `docs/agent-handoffs/2026-04-22-finish-qt-ui.md`
- Docket Graphite polish separately before starting C1 code work
- Commit the doc-only split cleanly

## What I Did
- Bootstrapped from the repo-owned executor docs under `agents/base/`.
- Verified the Qt handoff against the live UNC checkout and created `finish-qt-ui`.
- Added a `v0.16.5 — Qt Polish (Graphite)` entry to the Next Releases table in `MILESTONES.md`.
- Added a dedicated `v0.16.5` milestone detail block pointing at `assets/stitch_bif_ui/obsidian_graphite/DESIGN.md`.
- Updated the latest `SESSION_HANDOFF.md` summary so v0.16.0 stays the functional editor tranche and Graphite stays deferred to `v0.16.5`.
- Marked Graphite in `FEATURES.md` as docketed work instead of loose backlog.
- Added a concise `CHANGELOG.md` unreleased note for the milestone split.
- Ran commit-gate checks in a Qt-ready shell:
  - `. .\\setup_qt_env.ps1; cargo build`
  - `. .\\setup_qt_env.ps1; cargo clippy -- -D warnings`
  - `. .\\setup_qt_env.ps1; cargo fmt --check`

## Issues Found
- The handoff was written against a newer UNC checkout; the older local `D:` clone is missing both `agents/` and `docs/agent-handoffs/`.
- The original C0 tweak to `SESSION_HANDOFF.md` landed in an older historical section first; moved the milestone note to the top current-session summary instead so history stays intact.
- The handoff markdown itself is still untracked in this checkout; keeping it out of the commit.

## Current State
- C0 is complete and still doc-only.
- Graphite is explicitly parked in `v0.16.5`; `v0.16.0` remains the active functional milestone.
- Pre-commit checks are green in the Qt-loaded shell.
- Branch `finish-qt-ui` is ready for a scoped docs commit before any C1 code work.

## Learnings
- The repo-owned bootstrap (`agents/base/PROJECT.md` + `agents/base/HANDOFF_CONTRACT.md`) is the right starting point for Codex execution when handoffs live in-repo.
- A hard stop after C0 is worth keeping on large sequential handoffs; it catches roadmap/doc placement issues before they get buried under code work.
- For this repo, "function before form" is concrete scheduling policy, not just taste — the Graphite split belongs in roadmap docs, handoff context, and release notes together.

## Next Session
- Commit the C0 docs on `finish-qt-ui`.
- Start C1 only after the doc split is locked in.
- Keep the untracked handoff file out of the commit unless it is intentionally being versioned later.

## Wiki Links
- [[design-philosophy]] — Graphite / "Quiet Confidence" is the deferred styling source being split into `v0.16.5`
- [[003-hybrid-usd-workflow]] — the v0.16 editor tranche remains about authored edit flows, not UI skin polish

---

## Session Duration
~2 hr

## Goals
- Close out v0.15.0 on `main`
- Land the planned post-merge cleanup batch
- Cut release docs and prep the repo for v0.16

## What I Did
- Added a cached selected-prim stack snapshot on `BifShellState` so the Qt property inspector no longer re-fetches the full prim stack for every composition-arc row.
- Updated ortho preset application to refresh camera aspect from the live viewport rect before switching projection.
- Gated the Qt timeline `QTimer` so playback pauses while the app is hidden/minimized/inactive and resumes when the app is active again.
- Reworked the Qt scene browser model to lazy-populate children one level at a time via `hasChildren` / `canFetchMore` / `fetchMore`.
- Prevented the demo tree from re-seeding after a real stage has been loaded once and later closed.
- Bumped release-facing docs and split `CHANGELOG.md` for `v0.15.0`.
- Bumped the workspace package version to `0.15.0` so `BIF_QT_VERSION` matches the shipped release.
- Repaired the release validation suite by replacing deleted HDR fixtures with self-contained test data and updating stale USD/export readback tests to load payload-backed caches before asserting on meshes, instancers, materials, or lights.

## Issues Found
- The ship handoff originally referenced `agents/workflows/release-ship.md` before that workflow existed in the repo; the release follow-up added the canonical workflow doc so future handoffs resolve cleanly.
- `Cargo.toml` was still at `0.14.0` even though the handoff expected a `v0.15.0` ship state; fixed as part of the release closeout.
- `README.md`, `MILESTONES.md`, and `ROADMAP_DETAIL.md` needed a second pass after the tag-prep work because they still described the pre-ship Qt migration roadmap instead of the shipped v0.15.0 state.

## Current State
- `main` is prepared as the v0.15.0 baseline.
- Qt cleanup items 1a, 1b, and 1c from the ship handoff are implemented in the working tree.
- Release docs are updated for a v0.15.0 ship state across `CLAUDE.md`, `CHANGELOG.md`, `README.md`, `MILESTONES.md`, `ROADMAP_DETAIL.md`, and `SESSION_HANDOFF.md`; v0.16.0 is the next active milestone.
- The release validation suite is green in a Qt/USD-ready shell after fixing stale HDR fixture and payload-loading test assumptions.

## Learnings
- The property inspector hot path was the stack rows, not the attribute table itself; caching the selected prim stack removes the most obvious redundant USD calls without widening the qinvokable API.
- Scene-browser lazy loading needs the widget behavior to cooperate; removing unconditional `expandAll()` at startup is part of making `fetchMore` real instead of cosmetic.
- Release handoffs need one more repo-state sanity pass than normal implementation plans; version metadata and release docs can drift independently of the feature code.

## Next Session
- Push `main` plus the local annotated `v0.15.0` tag when ready.
- Confirm the Pages deploy succeeds on the main-branch push.
- Start v0.16.0 kickoff work on Edit Operations + Save from a clean baseline.

## Wiki Links
- [[2026-04-22-shared-agent-config]] — agent-config work ships inside the v0.15.0 release notes
- [[007-shell-state-to-viewport-bridge]] — Qt shell/runtime bridge still underpins the shipped v0.15.0 path
