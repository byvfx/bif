# Development Log - 2026-04-30

## Session Duration
Multi-hour autonomous cleanup session.

## Goals
- Execute the `docs/agent-handoffs/2026-04-29-v0.16.1-bug-fix-sweep.md` follow-up plan.
- Bring the resulting local commits back into `$bif-commit` shape.
- Update the repo-owned release tracking docs before any release merge or tag.

## What I Did

### v0.16.1 bug-fix sweep
- Landed seven focused local commits on `v0.16.1-followups` for the v0.16.0 follow-up list.
- Fixed save-error reporting so USD/TfError detail reaches Rust and the UI.
- Hardened Qt callback and modal paths with `QPointer` guards and deferred confirmation dialogs.
- Logged poisoned stage mutex paths instead of silently ignoring them.
- Filtered empty scene-browser child paths at the `CompositeProvider` source.
- Centralized `CString` conversion in `cpp_bridge.rs` through `cstr(...)`.
- Preserved recorded edit layers across undo/redo after edit-target switches.
- Added the View -> Grid toggle and threaded display scale into gizmo and outline sizing.

### Commit cleanup
- Audited the local history against `$bif-commit`.
- Rewrote the seven local commit messages to remove legacy AI co-author trailers and literal escaped newline text.
- Kept `Cargo.lock` out of the docs cleanup because its version diff predates this commit scope.

### Repo upkeep
- Moved completed v0.16.1 follow-up items out of `BUGLIST.md` active bugs.
- Added `[Unreleased]` changelog notes for the bug sweep.
- Prepended this local branch state to `SESSION_HANDOFF.md`.
- Added this devlog entry so the cleanup is traceable from the repo.

## Validation
- `$bif-commit` cleanup checks passed before this docs commit: `cargo fmt --check`, `. .\setup_qt_env.ps1; . .\setup_usd_env.ps1; cargo build`, and `. .\setup_qt_env.ps1; . .\setup_usd_env.ps1; cargo clippy -- -D warnings`.
- `cargo fmt --check` passed during the sweep.
- `. .\setup_qt_env.ps1; . .\setup_usd_env.ps1; cargo build --all` passed during the sweep.
- `. .\setup_qt_env.ps1; . .\setup_usd_env.ps1; cargo clippy --all -- -D warnings` passed during the sweep.
- `. .\setup_usd_env.ps1; cargo test -p bif_math` passed.
- `. .\setup_usd_env.ps1; cargo test -p bif_renderer` passed.
- `. .\setup_usd_env.ps1; cargo test -p bif_viewport` passed.
- `. .\setup_usd_env.ps1; cargo test -p bif_core -- --test-threads=1` passed.

## Issues Found
- The first local commits did not follow `$bif-commit`: they carried legacy AI co-author trailers, literal `\n\n` text in commit bodies, and missed the repo upkeep docs.
- PowerShell sandbox path mapping failed in this environment, so direct PowerShell execution was needed for repo commands.
- Manual dogfood smoke and synthetic HiDPI visual verification are still pending before a `v0.16.1` release.

## Current State
- Branch: `v0.16.1-followups`.
- Code sweep: committed locally with cleaned commit messages.
- Docs cleanup: in progress in this entry and the linked tracking docs.
- Remaining release work: manual dogfood, synthetic HiDPI visual verification, release prep, merge, tag, and push.

## Learnings
- Run `$bif-commit` before committing, not after, so the staged set, validation, docs, and message shape stay aligned.
- Stashing unrelated dirty files before a local message rewrite kept the rewrite scoped to history and avoided dragging `Cargo.lock` into cleanup.
- For this repo, Qt and USD validation should keep using the setup scripts in the same shell invocation.

## Next Session
- Dogfood `cargo run -p bif_viewer` against the v0.16.0 checklist plus the v0.16.1-specific manual checks.
- Run synthetic HiDPI visual verification or add a temporary local override, then remove it before release.
- Prepare the `v0.16.1` release merge, tag, and push only after manual checks are complete.

## Wiki Links
- `docs/agent-handoffs/2026-04-29-v0.16.1-bug-fix-sweep.md`
- `docs/agent-handoffs/2026-04-27-v0.16-c4b-edit-features.md`
- `docs/agent-handoffs/2026-04-24-v0.16-c4a-edit-foundation.md`
- `wiki/journal/2026-04-22-shared-agent-config.md`
