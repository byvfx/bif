---
title: Devlog 2026-04-13 — v0.15.0 kickoff + Phase 0 gate passed
date: 2026-04-13
tags: [devlog, v0.15, qt, spike, adr-006]
---

# Development Log — 2026-04-13 (session 2)

## Session Duration

~3 hours (post v0.14.0 release, same day)

## Goals

1. Start v0.15.0 Qt migration per plan `iridescent-soaring-hamster.md`.
2. Install Qt 6.8 LTS + MSVC 2022 64-bit components.
3. Execute Phase 0 spike gate (wgpu into QWidget).
4. ADR + commit if gate passes.

## What Was Done

### Qt 6.8.3 LTS install

- Installed via official Qt Online Installer (LGPL, OSS license) at `C:\Qt\6.8.3\msvc2022_64\`. Minimal component set: MSVC 2022 64-bit, Qt Creator, CMake_64, Ninja. Skipped Charts/Multimedia/Quick 3D/Shader Tools/Task Tree.
- Authored `setup_qt_env.ps1` mirroring `setup_usd_env.ps1` style: sets `Qt6_DIR`, `CMAKE_PREFIX_PATH`, `PATH`, `QT_PLUGIN_PATH`, `QML2_IMPORT_PATH`. Prints qmake version on successful load.

### Branch + workspace wiring

- `git checkout -b v0.15-qt` — migration branch. Main stays on v0.14.0 shippable until merge at Phase H.
- Added `crates/bif_qt_spike` to workspace members. Added workspace deps: `raw-window-handle 0.6`, `cxx 1.0`, `cxx-qt 0.7`, `cxx-qt-lib 0.7` (qt_full feature), `cxx-qt-build 0.7`, `qt-build-utils 0.7`.

### Phase 0 spike — `crates/bif_qt_spike/`

Minimum viable wgpu-into-QWidget proof. Final shape:

| File | Purpose |
|---|---|
| `Cargo.toml` | cxx + wgpu + raw-window-handle + qt-build-utils (build dep). `cxx-qt` declared but unused in spike. |
| `build.rs` | `QtBuild::new(["Core","Gui","Widgets"])` → moc on `render_widget.h` → `cxx_build::bridge("src/bridge.rs")` with MSVC+Qt flags (`/Zc:__cplusplus`, `/permissive-`, `/utf-8`, `/EHsc`). |
| `cpp/render_widget.{h,cpp}` | QWidget subclass with `WA_NativeWindow`, `WA_PaintOnScreen`, `WA_OpaquePaintEvent`, `WA_NoSystemBackground`, `paintEngine() → nullptr`. Signals: `resized`, `frameRequested`, `surfaceReady`. Exposes `nativeWinId()` + `nativeHInstance()` + `pixelWidth/Height()`. Internal 16ms QTimer drives continuous updates. |
| `cpp/app_main.{h,cpp}` | `qt_spike_run(SpikeCallbacks*)` entry. Constructs QApplication (static argc/argv), QMainWindow, RenderWidget, connects signals to cxx-generated trampolines. Blocks until `QApplication::exec()` returns. |
| `src/bridge.rs` | Plain `#[cxx::bridge]`. Rust-opaque `SpikeCallbacks`. Four Rust functions called from C++: `spike_on_surface_ready / resize / frame / shutdown`. |
| `src/renderer.rs` | wgpu::Instance (PRIMARY backends) → Surface from `RawWindowHandle::Win32` built from HWND+HINSTANCE → HighPerf adapter → Device+Queue → sRGB surface config → triangle pipeline. `render()` draws clear + 3-vert triangle. |
| `shaders/triangle.wgsl` | Hardcoded 3 verts, vertex-color interpolation. |
| `src/main.rs` | `env_logger` → allocate `SpikeCallbacks` on stack → `qt_spike_run(&mut cb)`. |

### Build iteration — 4 errors, all fixed

1. `qt.cargo_link_libraries(&mut std::io::stdout())` → wrong type. Fixed: pass `&mut builder` after configuring cxx_build.
2. `qt.moc(...)` needs `&mut self` → made `qt` binding mutable.
3. `fatal error C1189: Qt requires /Zc:__cplusplus` → added MSVC recipe (`/Zc:__cplusplus`, `/permissive-`, `/utf-8`, `/EHsc`).
4. `QT_NO_KEYWORDS` breaking `signals:` / `emit` in header → removed the define (not needed without cxx-qt macros).

Cold build: ~50s (cxx + wgpu-hal + Qt moc + 3 C++ .cpp). Incremental: ~6s.

### Phase 0 gate PASSED

Launched `bif_qt_spike.exe` (167 MB RSS, pid 30076) — Qt window opened with dark-grey background and a three-colored triangle. Resize works cleanly (triangle reflows with aspect). Screenshot captured at `assets/screenshots/qt_spike_wgpu.PNG`. Process closes cleanly on window close.

**All gate criteria met. Phase A unblocked.**

### ADR-006

Authored `wiki/architecture/adr/006-qt-via-cxx-qt.md` documenting:

- Binding decision (cxx-qt 0.7 + qt-build-utils 0.7 + Qt 6.8.3 LTS) with alternatives table.
- Licensing choice (LGPL + dynamic linking).
- wgpu QWidget embedding pattern — the full `paintEngine() == nullptr` + attribute recipe.
- MSVC + Qt gotchas captured from the spike (4 items).
- Migration strategy (shell-first, panel-by-panel, merge at Phase H).

## Learnings

- **cxx-qt vs plain cxx decision can defer.** The spike used plain `cxx` + `qt-build-utils` to isolate the wgpu-QWindow question from cxx-qt macro maturity. Phase A will commit to cxx-qt for Rust-side QObjects. Low-risk because cxx-qt is strictly a superset of cxx + Qt.
- **`/Zc:__cplusplus` is the first thing to add when MSVC meets Qt 6.** Without it, `qcompilerdetection.h` fatal-errors before any real C++ compiles.
- **`QT_NO_KEYWORDS` is optional and has real cost.** Without it, `signals:` and `emit` work normally; with it, every signal/slot declaration becomes `Q_SIGNALS:` / `Q_EMIT`. cxx-qt recommends defining it to avoid Rust keyword collision with `slots`. Phase A choice.
- **`paintEngine() const override { return nullptr; }`** is the key line that makes wgpu-in-QWidget work. Qt's raster engine never touches the widget, so wgpu owns the HWND's pixels entirely.
- **QWidget::winId() on Windows returns HWND directly** — no cast gymnastics. Wrap into `NonZeroIsize` → `Win32WindowHandle` → `RawWindowHandle::Win32`. `hinstance` comes from `GetModuleHandleW(nullptr)` on the C++ side.
- **QApplication lifetime + argc storage.** `QApplication(int&, char**)` keeps a reference to argc. Spike sidestepped Rust-side lifetime juggling by constructing `static char arg0[]; static int argc = 1;` in the C++ `qt_spike_run()`. Phase A/B can revisit.

## Issues Found

None — spike passed first run after the 4 compile fixes above. No runtime issues, no DPI bugs on primary monitor (single-monitor test only; need to re-verify on HiDPI).

## Current State

- Branch: `v0.15-qt` (off main at `1b20f4a`).
- Main: untouched, still v0.14.0 shippable.
- New files: `setup_qt_env.ps1`, `crates/bif_qt_spike/*`, `wiki/architecture/adr/006-qt-via-cxx-qt.md`, `assets/screenshots/qt_spike_wgpu.PNG`.
- Workspace builds clean with Qt env loaded (`cargo build`, `cargo fmt --check`, `cargo clippy -p bif_qt_spike -- -D warnings` all ✅).

## Next Session

1. **Phase A — `crates/bif_qt/` scaffolding** (~4h budget):
   - New crate depending on `bif_core`, `bif_renderer`, `cxx-qt 0.7`, `cxx-qt-lib 0.7`.
   - `build.rs` with `cxx_qt_build::CxxQtBuilder` (first real cxx-qt macro use).
   - Port 65 theme colors from `bif_viewport/src/theme.rs` to QPalette + Qt stylesheet.
   - `QMainWindow` subclass — menu bar, status bar, central placeholder, 4 dock slots.
2. Decide whether to flip `QT_NO_KEYWORDS` on in Phase A (affects how signals are declared in C++).
3. Verify Qt 6.8 LTS + cxx-qt 0.7 compatibility with a minimal `#[cxx_qt::bridge]` before committing to Phase A structure.

## Wiki Links

- [[006-qt-via-cxx-qt]] — ADR for this migration, authored this session
- [[002-egui-temporary-ui]] — the ADR that scoped egui as temporary; this session delivers on it
- [[005-layer-aware-read-model]] — v0.14 ADR that made `bif_core` UI-agnostic; v0.15 is the payoff

---

# Development Log — 2026-04-13 (session 3)

## Session Duration

~2 hours (same day, immediately after Phase 0 commit)

## Goals

1. Phase A — `crates/bif_qt/` scaffolding per plan `iridescent-soaring-hamster.md`.
2. Validate that `cxx-qt 0.7` macros compile against Qt 6.8.3 LTS + MSVC 2022.
3. Port the 34 egui theme color constants to a Qt-style palette + stylesheet generator.
4. Assemble a minimum QMainWindow shell that launches and demonstrates the 4 dock slots.

## What Was Done

### `crates/bif_qt/` crate created

- **`Cargo.toml`** — depends on `bif_core`, `bif_renderer`, `cxx-qt 0.7`, `cxx-qt-lib 0.7` (`qt_full` feature), `wgpu`, `raw-window-handle`, `anyhow`, `log`, `env_logger`. Library target + `bif_qt_shell` binary for dogfooding.
- **`build.rs`** — switches from plain `cxx_build` (spike) to `cxx_qt_build::CxxQtBuilder`. Qt modules: Widgets + Gui. MSVC flags carried over (`/Zc:__cplusplus`, `/permissive-`, `/utf-8`, `/EHsc`).
- **`src/lib.rs`** — exports `app`, `main_window`, `theme`. `#![allow(clippy::missing_safety_doc)]` at crate scope (required — clippy can't introspect docs on declarations inside `#[cxx_qt::bridge]` macro expansion).
- **`src/main_window.rs`** — first real `#[cxx_qt::bridge]` in BIF. Declares `BifShellState` QObject with `#[qproperty(QString, title)]` + `#[qproperty(QString, status_message)]` + `#[qinvokable] fn describe(self: Pin<&mut Self>) -> QString`. `BifShellStateRust` struct with `QString` fields (no `Default` impl on QString, so manual `Default for BifShellStateRust`). `impl qobject::BifShellState { fn describe(...) }` lives outside the bridge per cxx-qt 0.7 convention.
- **`src/app.rs`** — `run() -> Result<i32>` initializes env_logger, calls `crate::main_window::qobject::bif_qt_run_shell()`. Blocks until Qt event loop exits.
- **`src/theme.rs`** — port of `bif_viewport/src/theme.rs` to UI-agnostic `Color(u8, u8, u8, u8)` + `to_qt_rgba_string()` helper. All 34 named constants mirror egui names. `qt_stylesheet()` fn generates a ~80-line Qt stylesheet covering QMainWindow, QDockWidget, QMenuBar, QMenu, QStatusBar, QPushButton, QLineEdit, QTreeView, QTableView, QListView, QHeaderView, QTabWidget, QTabBar, QScrollBar. 4 unit tests on constants + stylesheet formatting.
- **`src/bin/bif_qt_shell.rs`** — Phase A/B dogfood binary. Deleted at Phase F.

### C++ side

- **`cpp/window_builder.h/.cpp`** — `extern "C" int bif_qt_run_shell()` constructs `QApplication` (static argc/argv) + `QMainWindow` (1440x900) + `BifShellState` parented to window + File/View/Help menu bar with Ctrl+N/O/S/Shift+S/Q/1-4/\\ accelerators + 4 `QDockWidget` placeholders (Scene Browser, Layer Stack on left; Property Inspector on right; Node Graph at bottom) + central viewport placeholder + status bar. Movable/floatable/closable docks.
- cxx-qt generated header: `#include "bif_qt/src/main_window.cxxqt.h"` — crate + src prefix convention.

### Compile iteration — 3 errors, all fixed

1. `impl qobject::BifShellState { ... }` INSIDE the bridge → "expected an empty impl block". Moved the impl outside the bridge (cxx-qt 0.7: `#[qinvokable]` declared in `extern "RustQt"`, implemented in a regular impl block outside).
2. `self.rust()` unresolved → missing `use cxx_qt::CxxQtType;` trait import for accessor method.
3. Link error `unresolved external symbol cxxbridge1$194$bif_qt_run_shell` → had plain `#[cxx::bridge]` in `app.rs` alongside the cxx-qt bridge. Cannot mix — `cxx_qt_build::CxxQtBuilder` only processes `#[cxx_qt::bridge]`. Merged the extern decl into the single cxx-qt bridge.

Plus two lint fixes: `# Safety` section added to `bif_qt_run_shell` doc; `#![allow(clippy::missing_safety_doc)]` at crate scope for the macro-expanded form clippy can't see.

### Verification

Launched `bif_qt_shell.exe` — 30 MB working set, correct title, all 4 placeholder docks visible and movable, menu bar interactive. User confirmed visually: "this works well." No crashes, clean exit on window close.

## Learnings

- **cxx-qt 0.7 `#[qinvokable]` vs plain cxx fn signatures.** Declared in `extern "RustQt"` inside the bridge, implemented in `impl qobject::TheObject { fn foo(...) }` OUTSIDE the bridge. Non-empty impl blocks inside the bridge are a compile error. This is opposite to how cxx works for plain `extern "C++"` types.
- **cxx-qt's `CxxQtType` trait is the gateway to Rust-side state.** `.rust()` for `&BifShellStateRust`, `.rust_mut()` for `&mut`. Must be in scope.
- **cxx-qt-generated include path convention:** `<crate_name>/src/<bridge_file>.cxxqt.h`. For our case: `bif_qt/src/main_window.cxxqt.h`.
- **Can't mix cxx + cxx-qt bridges in one crate.** `cxx_qt_build::CxxQtBuilder` only processes `#[cxx_qt::bridge]` modules. Any `#[cxx::bridge]` module's Rust↔C++ glue never compiles, manifesting as a link-time unresolved-external. Merge extern decls into one cxx-qt bridge.
- **`#![allow(clippy::missing_safety_doc)]` is needed at crate scope** for cxx-qt bridges. Clippy sees the macro-expanded function forms, not our `# Safety` doc comments on the declarations. Documenting anyway is still worth it for humans reading the source.
- **C++ continues to own widget tree assembly (QMainWindow/QDockWidget/QMenuBar) for Phase A**, not because cxx-qt can't do it but because cxx-qt-lib's QtWidgets wrappers are thin and the Rust boilerplate wouldn't pay off here. Phase B/C may revisit if Rust-side QObject adoption grows.

## Issues Found

None — Phase A scaffolding worked first run after the 3 compile fixes + 2 lint fixes above.

## Current State

- Branch: `v0.15-qt`. `main` still v0.14.0 shippable.
- `crates/bif_qt/` builds clean (`cargo build`, `cargo clippy -p bif_qt --all-targets -- -D warnings`, `cargo fmt --check`).
- `bif_qt_shell` binary launches a real Qt shell — 30 MB working set vs spike's 167 MB (less geometry + no wgpu).
- Workspace `Cargo.lock` gained 8 cxx-qt transitive deps (cxx-gen, cxx-qt, cxx-qt-build, cxx-qt-gen, cxx-qt-lib, cxx-qt-macro, clang-format, convert_case, indoc).

## Next Session

**Phase B — Qt shell (~12h budget):**

1. Port `bif_qt_spike/cpp/render_widget.{h,cpp}` → `bif_qt/cpp/render_widget.{h,cpp}`. Make it the central widget of QMainWindow. Wire `surfaceReady` / `resized` / `frameRequested` through a cxx-qt QObject that owns `bif_renderer::Renderer`.
2. Apply `theme::qt_stylesheet()` via `QApplication::setStyleSheet` — pass string from Rust through an extern "Rust" fn or QString property.
3. File menu wiring (File/New/Open/Save/SaveAs/Quit) → cxx-qt `#[qinvokable]` methods.
4. Command palette (Ctrl+P) — `QDialog` with `QLineEdit` + `QAbstractListModel` backed by Rust `CommandRegistry` (new in `bif_core`). Fuzzy matching.
5. Breadcrumb bar above viewport — `QToolBar` with segment buttons emitting `AppEvent::PrimSelected`.
6. Four workspace presets (Assembly/Lighting/Materials/Render) — `QMainWindow::saveState()` byte arrays in `QSettings`. Ctrl+1/2/3/4.
7. First-launch screen — full-window widget when no stage loaded.
8. Zen mode — Ctrl+\ toggles all dock visibility.

## Wiki Links

- [[006-qt-via-cxx-qt]] — this session added Phase A results; ADR gotchas section is accurate
- [[002-egui-temporary-ui]] — one step closer to its fulfillment
