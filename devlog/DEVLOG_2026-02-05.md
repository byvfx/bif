# Development Log - 2026-02-05

## Session Duration
~30 min

## Goals
- Implement IBL performance follow-up fixes from code review

## What I Did
- Pre-allocated 5 uniform buffers for prefilter params in `ComputeIbl::new()`
- Changed `encode_prefilter()` to use `queue.write_buffer()` instead of creating new buffers per mip
- Added `device.poll(Maintain::Wait)` after GPU submit for accurate timing
- Added comment documenting wgpu implicit barriers between passes
- Removed unused `DeviceExt` import
- Skipped .tx conversion for HDRIs (no benefit - we load all pixels anyway)
- Parallelized importance sampling CDF build with rayon

## Learnings
- `queue.write_buffer()` + pre-allocated buffers avoids per-frame allocations
- `device.poll(Wait)` blocks CPU but gives accurate GPU timing measurement
- wgpu handles implicit barriers between compute passes automatically

---

## Session 2 Duration
~30 min

## Goals
- Constrain 3D viewport to remaining GUI space after egui panels

## What I Did
- Added `ui_top_panel_height` field to `Renderer` struct
- Added placeholder top panel (28px, "BIF" label) via `egui::TopBottomPanel::top`
- Added `viewport_rect()` helper: returns (x, y, w, h) after subtracting all 4 panels
- Added `viewport_scissor()` helper: bounds-safe u32 version for `set_scissor_rect`
- Applied `set_viewport()` + `set_scissor_rect()` to skybox, geometry, and Ivar render passes
- Updated camera aspect ratio to use viewport dims per-frame (not full window)
- Fixed `resize()` to use viewport-based aspect
- Refactored gnomon positioning to use `viewport_rect()` (now accounts for top panel)

## Learnings
- `set_viewport` maps NDC to pixel region; `set_scissor_rect` clips fragments outside it
- Depth buffer + surface stay full-window size; viewport/scissor handle the constraint
- egui panels must be created before side panels to reserve space correctly

---

## Session 3 Duration
~15 min

## Goals
- Fix RT playback stall: timeline not advancing in realtime mode

## What I Did
- Root cause: slider `.integer()` rounds sub-frame values every frame, triggering `changed()` which resets the playback anchor → timeline stuck oscillating
- Fix: gate slider writeback with `is_pointer_button_down_on()` so only user drags reset the anchor, not programmatic rounding
- Single edit in `crates/bif_viewport/src/lib.rs` (~line 4103)

## Learnings
- egui `Slider::integer()` quantizes every frame, fires `changed()` even for programmatic updates
- `Response::is_pointer_button_down_on()` distinguishes user interaction from programmatic changes

## Next Session
- Continue M17 viewport PBR work
- Consider making `CUBEMAP_SIZE` configurable if quality needs increase
- Optimize Ivar to only render viewport-sized pixels (currently wastes work behind panels)
