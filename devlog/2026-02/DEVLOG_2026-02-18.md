# Development Log - 2026-02-18

## Session Duration

~20 minutes

## Goals

- Fix "cannot sample empty range" panic when scale_min >= scale_max in scatter UI

## What I Did

- Added `normalized_scale_range()` helper in `scatter.rs` that swaps min/max if inverted
- Applied fix at all 3 `gen_range` call sites: `scatter_on_surface`, `generate_grid_points`, `generate_sphere_points`
- Added 4 regression tests: equal range, inverted range (grid, sphere, surface)
- Updated BUGLIST.md (cleared old resolved bugs)
- Applied code review feedback: moved normalization to `ScatterConfig::scale_range_normalized()` method, added `debug_assert` for NaN, hoisted call out of loops, added direct unit test

## Learnings

- `rand::gen_range(a..=b)` panics when `a > b` — always normalize before calling
- PowerShell needed for running tests (USD DLL loading doesn't work through bash PATH on Windows)

## Next Session

- M29 USD Export + Non-Destructive Layers (or choose next milestone)
- Consider adding similar range validation for `rotation_range` if it can go negative
