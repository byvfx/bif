# Development Log - 2026-02-12

## Session Duration

~30 min

## Goals

- Run VFX code review on last 2 commits (Ivar navigation preview)
- Fix all identified issues

## What I Did

### VFX Code Review (vfx-code-reviewer agent)

Reviewed commits `13a00e1` and `8e2bb41` (interactive Ivar navigation preview).
Found 2 critical, 4 important, and 4 suggestion-level issues.

### Fixes Applied (commit `ddf0e49`)

**Critical:**

1. GPU resource thrashing — was creating new texture/view/bind_group every frame during camera drag. Now skips recreation when dimensions unchanged.
2. Render loop ordering — settle timer checked `is_pass_in_flight()` before messages were polled, causing stale state. Moved poll_scene_build + poll_ivar_messages before settle timer.

**Important:**
3. Integer division aspect ratio drift — `vp_w / scale` truncates, causing aspect ratio jumps across refinement steps. Now uses `(vp_w / scale as f32).round()`.
4. Deduplicated `start_ivar_render` / `restart_ivar_at_scale` — `start_ivar_render` now delegates to `restart_ivar_at_scale(1)`, removing ~30 lines of duplicate logic.
5. Replaced `interaction_scale` (power-of-2) with `interaction_quality` (exponent) — eliminates fragile `trailing_zeros()` reverse mapping. Added `interaction_scale()` helper method.
6. Resize handler missing state reset — added `current_scale = 1` and `last_interaction_time = None` in `resize()`.

**Improvements:**
7. Skip AOV buffer allocation at reduced scale — alpha/depth/normal buffers not useful at 1/8 res.
8. Shorter settle timeout for coarse refinement — 150ms for scale>=4, 300ms for medium→full.
9. Added 7 unit tests for progressive refinement state machine.

### Files Modified

- `crates/bif_viewport/src/ivar_build.rs` — dedup, GPU thrash fix, aspect ratio fix
- `crates/bif_viewport/src/ivar_state.rs` — interaction_quality, AOV skip, tests
- `crates/bif_viewport/src/render.rs` — loop reorder, settle timer, quality exponent
- `crates/bif_viewport/src/lib.rs` — resize state reset

## Learnings

- GPU texture allocation during rapid interaction (60+ fps) causes allocation pressure — always check if dimensions changed before recreating
- Render loop ordering matters for state machines with settle timers — poll completion before checking timers
- Integer division for resolution scaling causes aspect ratio drift at non-power-of-2 viewport sizes
- `trailing_zeros()` is fragile for reverse-mapping power-of-2 values — store the exponent directly

## Next Session

- M21 Point Instancing + Scattering
- Consider extracting Ivar render state machine into its own method for clarity

---

## Session 2 Duration

~20 min

## Goals

- Fix remaining Ivar black flashes (first click, refinement steps, resize)
- Apply VFX code review improvements (buffer reuse, throttle helper)

## What I Did

### Black Flash Fix (commit `24b584e`)

Root cause: `reset_accumulation` created a black buffer on dimension change (full→interaction scale, refinement steps). Resize handler didn't fully reset Ivar state.

**Fixes:**

1. Nearest-neighbor resample old buffer to new dims instead of black — stale but visible during scale transitions
2. Reuse accum/AOV buffers with `.fill()` when pixel count matches — avoids ~25MB realloc per restart at full res
3. Extracted `should_restart()` throttle helper (50ms OR bucket-complete) — replaces duplicated 5-line inline check in 2 call sites
4. Full Ivar state reset in resize handler — was missing `cancel_flag` arc, `receiver`, `accumulated_samples`, `buckets_completed`, `last_camera_snapshot`, `render_start_time`

**Tests added:** 5 new (resample on dim change, buffer reuse, create-when-none, should_restart x2)

### Files Modified

- `crates/bif_viewport/src/ivar_state.rs` — RESTART_THROTTLE_MS, should_restart(), NN resample, buffer reuse, 5 tests
- `crates/bif_viewport/src/render.rs` — should_restart() at both throttle sites
- `crates/bif_viewport/src/lib.rs` — full resize reset

## Learnings

- Nearest-neighbor resample at 480x270 is ~130K iterations of simple index math, well under 1ms — cheap way to avoid visual artifacts
- `.fill()` on existing Vec avoids reallocation while still zeroing — important for hot paths called every restart
- Resize is fundamentally different from orbit: target dims unknown until next frame's viewport_rect(), so black-on-resize is acceptable

## Next Session

- M21 Point Instancing + Scattering
- Manual test: orbit, first-click, refinement, resize all flash-free
