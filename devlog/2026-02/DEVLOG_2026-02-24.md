# Development Log - 2026-02-24

## Session Duration
~1.5 hours

## Goals
- Fix export producing empty USD (no sublayer, no mesh data)
- UsdPrim + GraftBranches node implementation

## What I Did

### UsdPrim + GraftBranches Nodes (from prior session, committed now)
- Added `SceneNode::UsdPrim` with path/type/kind/specifier fields
- Added `SceneNode::GraftBranches` with 4-input merge and destination path
- C++ bridge: `usd_bridge_define_prim()` + `usd_bridge_set_prim_kind()` via UsdModelAPI
- Rust types: `UsdPrimType`, `UsdKind`, `UsdSpecifier` with Display + ALL arrays for UI combos
- `AuthoredPrim` struct for export pipeline
- `apply_graft_prefix()` for path remapping in export
- Export writes authored prims before xforms (parent prims must exist first)
- 4 new tests: authored prims, define scope, define xform, prim with kind
- Node graph UI: combo boxes for type/kind/specifier, green path labels
- Add node buttons in toolbar

### Export Sublayer Fix (this session)
- **Root cause:** `collect_export_context()` didn't collect source USD path from upstream UsdRead nodes. Export used `self.loaded_usd_path` which was fragile/None.
- `collect_export_context()` now returns 3-tuple: `(authored_prims, graft_prefix, source_usd_path)`
- Walks upstream BFS, captures `file_path` from `UsdRead { is_loaded: true }` nodes
- Canonicalizes to absolute path via `std::fs::canonicalize()`
- Auto-enables `as_sublayer` when upstream UsdRead found
- Falls back to `self.loaded_usd_path` for backwards compat
- UsdPrim default path changed from "/World" to "/root" (avoids overwriting imported scene root)
- `loaded_usd_path` in scene_loader now stored as canonical absolute path
- Added diagnostic logging: debug/info/warn around sublayer logic

## Learnings
- USD sublayer resolution is relative to the output layer's directory, not CWD — absolute paths are essential
- `DefinePrim("/World", "Scope")` in edit layer overrides sublayer's `/World` (stronger opinion wins)
- Walking the node graph upstream to collect context is the right pattern for export — avoids fragile global state

---

## Session 2: write_mesh + Prim Paths + Material Fix

### Duration
~2 hours

### Goals
- C++ write_mesh bridge for exporting procedural meshes
- Prim path fields on Primitive/PointInstancer nodes
- Fix material fallback index (unwrap_or(0) bug)
- Primitive scene input pass-through

### What I Did

#### C++ write_mesh bridge
- `usd_bridge_write_mesh()` in usd_bridge.cpp: DefinePrim("Mesh"), sets subdivisionScheme="none", points, faceVertexCounts (all 3s), faceVertexIndices, optional normals (vertex interpolation), optional primvars:st UVs, extent (min/max bounding box)
- Rust FFI wrapper `UsdEditLayer::write_mesh` flattens Vec3 positions to f32 array, null pointers for empty normals/UVs

#### Export prototype meshes
- After writing PointInstancers, iterates proto_paths and writes actual mesh prims via `write_mesh`
- Uses `HashSet<String>` to deduplicate (same proto referenced by multiple instancers)
- Only writes BIF-created protos (skips USD-loaded ones via `starts_with('/')` check)
- `mesh_count` added to `ExportResult`

#### Prim paths on nodes
- `prim_path: String` on both Primitive and PointInstancer enum variants
- Auto-increment via `AtomicU32` counters: `/World/Cube1`, `/World/Sphere1`, `/World/instancer1`
- UI text edits in node body, green path labels
- Wired through: Primitive prim_path → prototype name via `Arc::make_mut`, PointInstancer prim_path → cloud.name

#### Primitive scene input
- Changed input_count 0→1, added ("scene", PinType::Scene) input pin
- Pass-through pattern same as Xform/UsdPrim

#### Material fallback fix
- **Bug:** `unwrap_or(0)` when proto.material is None picks first USD material (e.g., palm bark)
- **Fix:** Always append default grey `MaterialGpu` at end of table, use `unwrap_or(default_mat_index)`
- Fixed all 3 material_table build sites and all 7 material-related unwrap_or(0) calls

### Issues Encountered
- `cargo fmt` formatting diffs after implementation (long if-let lines)
- Type mismatch `String` vs `Arc<str>` for prototype name — fixed with `.as_str().into()`
- Cannot assign through `Arc` — fixed with `Arc::make_mut(proto)`
- USD DLL not found in bash for tests — used PowerShell with `setup_usd_env.ps1`

### Learnings
- `Arc::make_mut` is the idiomatic way to mutate shared data (clone-on-write if refcount > 1)
- AtomicU32 with Relaxed ordering is fine for auto-increment counters (no ordering dependency)
- Always run `cargo fmt` before committing — long if-let chains get reformatted

## Next Session
- Run VFX code reviewer on latest commit
- Validate exported USD in Houdini/usdview
- Ivar: apply Xform transforms to baked mesh_data
- Xform prim_filter implementation
- Consider M26 Denoising
