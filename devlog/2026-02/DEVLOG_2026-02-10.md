# Development Log - 2026-02-10

## Session Duration
~2 hours

## Goals
- Fix 5 bugs from BUGLIST.md (surface error, grid toggle, node deletion, multi-object, camera dropdown)

## What I Did

### Phase 1: Surface Error on Minimize
- Combined `SurfaceError::Lost` and `Outdated` arms to both reconfigure swapchain
- Added 0x0 window size check before rendering (skip frame when minimized on Windows)
- Fixed: used `renderer` directly instead of re-borrowing `self.renderer`

### Phase 2: Grid Visibility Toggle
- Added `show_grid: bool` field to Renderer (default true)
- Wrapped `grid.render()` in `if self.show_grid`
- Added checkbox in top toolbar panel

### Phase 3: Node Deletion
- Added `SelectNode(NodeId)` and `DeleteNode(NodeId)` event variants
- Implemented `has_node_menu` + `show_node_menu` on SnarlViewer — right-click context menu with "Delete"
- Click detection on node body → SelectNode event → sets `selected_node`
- Delete key / Backspace / "Del Selected" button all work via `delete_selected()`

### Phase 4: Multi-Object Persistence + Undo (biggest change)
- Added `working_scene: Scene` to Renderer — accumulates all primitives + USD objects
- Added `reload_working_scene()` — rebuilds all GPU state from working_scene (multi-draw, culling, materials, pick scene)
- Refactored `load_primitive()` to add to working_scene instead of creating isolated scenes
- Added `remove_prototype()` and `remove_instance()` to Scene (re-indexes remaining IDs)
- Added `SceneOp`, `CreatePrimitiveCommand`, `DeletePrimitiveCommand` to undo system
- Node deletion now removes prototype from working_scene via node_proto_map
- USD load merges prototypes/instances into working_scene
- Pending scene ops from undo/redo processed at start of render loop

### Phase 5: Camera Dropdown Fix
- Verified no additional code needed — Phase 4 persistence handles everything
- Camera dropdown reads `self.scene_cameras` dynamically (populated from working_scene)
- Stale camera index check already present in reload_working_scene

## Learnings
- egui_snarl 0.5 has `has_node_menu`/`show_node_menu` — better than manual X buttons
- NLL (Non-Lexical Lifetimes) allows reusing `renderer` after `render()` returns error
- `Snarl::remove_node` handles disconnecting wires automatically
- Working scene pattern (accumulate → rebuild) is cleaner than trying to do incremental GPU updates

### Phase 6: Code Review Critical Fixes
- Fix 1A: `delete_selected()` returns `Option<NodeId>` instead of removing directly — keyboard/button delete now emit `DeleteNode` events for scene cleanup
- Fix 1B: `remove_instance` camera logic — retain first (remove at index), then decrement higher
- Fix 1C: `remove_prototype` camera logic — track removed instance indices, retain cameras not at those indices, re-index by shift count
- Fix 2D: USD merge remaps prototype IDs and camera instance indices via `proto_offset`/`instance_offset`
- Fix 2B: Instance buffer capacity check — truncate at MAX_INSTANCES with warning

### Phase 7: Second Code Review Fixes
- Fix stale `node_proto_map` after deletion — only re-index on successful removal, re-insert on failure
- Fix `load_scene_data` buffer overrun — same truncation pattern applied to 3rd code path
- Fix metadata mismatch — `num_instances`/`visible_count`/`num_triangles` use `write_count` in all paths

## Learnings (addendum)
- Buffer truncation must be applied to ALL code paths writing the same GPU buffer — easy to miss with 3 paths
- Side-effect ordering matters: don't re-index a map if the operation that triggered re-indexing failed

## Next Session
- Test multi-object workflow manually (create cube + sphere + camera, delete, undo)
- M21 Point Instancing + Scattering
- Consider: undo/redo for primitives doesn't update node_proto_map (minor edge case)
