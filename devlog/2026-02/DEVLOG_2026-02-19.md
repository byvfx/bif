# Development Log - 2026-02-19

## Session Duration

~1.5 hours

## Goals

- Fix scatter dirty propagation (primitive size change → recompute)
- Hide scatter surface geometry from viewport
- Replace 1px point dots with sized/colored billboard circles

## What I Did

### Fix 1: Recursive Dirty Propagation

- Extracted `mark_node_dirty` from `SceneNodeViewer` to a `pub(crate)` free function
- Added `propagate_dirty()` — BFS walk of all downstream output connections
- Replaced 3 single-level dirty walks (CreatePrimitive, ScatterPointsCompute, DeleteNode)
- Chain `Primitive → ScatterPoints → PointInstancer` now recomputes correctly

### Fix 2: Hide Scatter Surface Geometry

- Added `scatter_surface_proto_ids: HashSet<usize>` and `node_scatter_surface_map: HashMap<NodeId, usize>` to Renderer
- Surface proto tracked on scatter compute, cleared on reconnect/delete
- `reload_working_scene()` merges scatter surfaces into `hidden_proto_ids` set
- Replaced all `instanced_proto_ids` filter references with `hidden_proto_ids`

### Fix 3a: Billboard Quad Point Rendering

- Switched pipeline topology from `PointList` to `TriangleStrip`
- Vertex shader uses `instance_index` for point lookup, `vertex_index` (0-3) for quad corners
- Screen-space quad offsets scaled by `point_size` pixels / viewport dimensions
- Fragment shader discards outside unit circle for round points
- Added `viewport_width`/`viewport_height` to `PointParams` uniform
- `update_params()` method for per-frame viewport sync without re-uploading points
- Draw call: `draw(0..4, 0..point_count)` — 4 verts per quad, one instance per point

### Fix 3b: Per-Node Color/Size UI

- Added `point_size: f32` and `point_color: [f32; 4]` fields to `ScatterPoints` variant
- New `PointPreviewUpdate` event (cosmetic only, no recompute)
- `DragValue` slider for size (1-20px), `color_edit_button_rgba_unmultiplied` for color
- Handler writes to `point_preview.point_size`/`color` and pushes params to GPU
- Scatter compute syncs node's color/size to preview renderer

### Files Modified

| File | Changes |
|------|---------|
| `node_graph.rs` | Free fn `mark_node_dirty` + `propagate_dirty`, ScatterPoints fields, UI, event |
| `render.rs` | `propagate_dirty` calls, surface tracking, `PointPreviewUpdate` handler, viewport dims |
| `scene_loader.rs` | `hidden_proto_ids` = instanced ∪ scatter surfaces |
| `lib.rs` | New Renderer fields for scatter surface tracking |
| `point_preview.rs` | Billboard quads, `point_size` field, `update_params()` |
| `point_preview.wgsl` | Billboard vertex shader, circle mask fragment shader |

## Learnings

- wgpu `PointList` renders 1px with no size control — `@builtin(point_size)` is not standard WebGPU
- Billboard quads via instanced TriangleStrip is the standard approach for sized points
- NDC offset = pixel_offset / viewport_size * 2.0, multiplied by clip_pos.w for perspective correctness
- BFS dirty propagation is cleaner than manually checking each node type at each level

---

## Session 2: Code Review Fixes

### Duration

~30 minutes

### Goals

- Fix 3 issues from VFX code review of scatter fixes commit

### What I Did

#### Fix 1: Gate per-frame GPU writes

- Added `point_preview_last_vp: (f32, f32)` and `point_preview_params_dirty: bool` to Renderer
- Moved `update_params()` out of render pass to before it
- Only writes GPU buffer when dirty flag set or viewport size changed
- Removed per-frame `update_params` call from inside Vulkan render pass

#### Fix 2: Add node_id to PointPreviewUpdate

- Added `node_id: NodeId` field to `PointPreviewUpdate` event
- Added doc comment explaining shared renderer limitation (last emitter wins)

#### Fix 3: Remove scatter_surface_proto_ids HashSet (ref-counting bug)

- Removed `scatter_surface_proto_ids: HashSet<usize>` from Renderer struct
- `reload_working_scene()` now rebuilds scatter surface IDs from `node_scatter_surface_map.values()`
- Eliminates bug where deleting one scatter node un-hides surface shared by another

#### Fix 4: Simplify propagate_dirty BFS

- Seeds queue with `start` node, single loop body with `current != start` guard
- Removes duplicated output-walking code from seed + loop

### Learnings

- HashSet for multi-source tracking is a ref-counting bug pattern — rebuild from authoritative map instead
- GPU buffer writes should be gated with dirty flags, not done per-frame for rarely-changing data

---

## Session 3: Live HDRI Rotation + Sharper Skybox

### Duration

~30 minutes

### Goals

- Fix HDRI rotation slider not updating live in Ivar (CPU path tracer)
- Fix pixelated viewport skybox (sampling prefiltered 128x128 instead of base cubemap)

### What I Did

#### Fix 1: Live HDRI Rotation in Ivar

- Added `_with_params` variants to `HdriEnvironment` (`sample_with_params`, `sample_direction_with_params`, `pdf_for_direction_with_params`) + `rotation()`/`intensity()` getters
- Added `hdri_rotation: Option<f32>` / `hdri_intensity: Option<f32>` to `RenderConfig`
- Updated all 6 env sampling calls in `ray_color` + `ray_color_with_aovs` to use overrides
- Added `hdri_rotation`/`hdri_intensity` fields to `IvarState`, synced on param update
- `UpdateHdriParams` now restarts Ivar when active
- CDFs are rotation-independent so no rebuild needed on rotation change

#### Fix 2: Sharper Viewport Skybox

- Stored base cubemap (`cubemap_texture`/`cubemap_view`) in `GpuEnvironment`
- Skybox bind group now receives base cubemap instead of prefiltered specular
- Renamed shader variable `prefiltered_map` → `env_cubemap`
- Bumped `CUBEMAP_SIZE` 256→512, `PREFILTER_SIZE` 128→256 (~17MB VRAM total)

### Files Modified (12)

| File | Changes |
|------|---------|
| `hdri.rs` | `_with_params` methods, getters |
| `renderer.rs` | `RenderConfig` override fields, 6 call sites updated |
| `ivar_state.rs` | `hdri_rotation`/`hdri_intensity` fields |
| `ivar_build.rs` | Pass overrides into progressive pass config |
| `lib.rs` | Sync ivar state on param update |
| `render.rs` | Restart ivar on param change, set initial values on load |
| `batch_render.rs` | Set overrides to None (use baked values) |
| `environment.rs` | Store base cubemap texture + view |
| `environment_manager.rs` | Wire cubemap_view into skybox bind group |
| `skybox.rs` | Rename param |
| `skybox.wgsl` | Rename variable |
| `compute_ibl.rs` | Resolution bump |

### Learnings

- HDRI importance sampling CDFs are built from raw pixel luminances, rotation-independent — can change rotation without rebuilding
- Skybox was sampling prefiltered specular cubemap (designed for roughness LOD) at mip 0 instead of the full-res base cubemap
- `Arc<HdriEnvironment>` prevented mutation — parameterizing at call time is cleaner than interior mutability

## Next Session

- Manual testing: load HDRI → adjust rotation slider → verify Ivar restarts with rotated env
- Manual testing: verify skybox looks sharper than before
- Consider M29 USD Export or next milestone

## Wiki Links

- [[wgpu-pipeline|wgpu Pipeline]] — wgpu rendering pipeline
- [[node-graph-system|Node Graph System]] — render and export nodes
- [[bif-usd-integration|BIF USD Integration]] — USD export with materials
- [[point-instancer|Point Instancer]] — instancer rendering
