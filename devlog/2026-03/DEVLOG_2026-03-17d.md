# Development Log - 2026-03-17d

## Session Duration
~2.5 hours

## Goals
- Implement architecture improvement plan: decompose Renderer God object into subsystems

## What I Did

### Phase 1: EventBus (highest leverage)
- Created `app_event.rs` with `AppEvent` enum (17 variants) + `EventBus` struct
- Replaced all 23 `egui::Id::new("...")` temp-data insert/read sites with typed events
- `dispatch_deferred_events()` (280 lines of untyped polling) -> `dispatch_events()` (~120 lines typed match)
- Modified `render_stats_panel` and `render_property_inspector` to accept `&mut EventBus` instead of `&egui::Context`
- Used `std::mem::take` pattern to extract EventBus from self before egui closure
- Gizmo hover converted from temp-data to local variable (frame-local state, not an event)
- 2 unit tests

### Phase 2: SelectionManager
- Created `selection.rs` consolidating 5 fields: selected_prim_path, selected_prim_properties, selected_instance_index, scene_browser_state, gizmo_state
- Added `gpu_highlight_id()` method replacing inline `map(|i| i as u32).unwrap_or(NO_SELECTION)` pattern
- Updated ~30 references across render.rs, scene_loader.rs, lib.rs, main.rs
- 3 unit tests

### Phase 3: IvarController - SKIPPED
- Tight coupling to Renderer (camera, scene, materials, GPU) makes extraction require 10+ reference params or traits
- Existing isolation in ivar_build.rs + ivar_state.rs is sufficient for now

### Phase 4: SceneManager (largest)
- Created `scene_manager.rs` consolidating 16 fields: working_scene, instances, instance_animations, mesh_data, scene_materials, scene_material, usd_stage, loaded_usd_path, scene_cameras, edit_state, undo_stack, texture_base_dir, plus animation buffers
- Added `Default` impl for `MeshData` (needed for SceneManager default init)
- Made `SceneInstances` public (was `pub(crate)`, needed for public SceneManager field)
- Updated ~200 references across render.rs, scene_loader.rs, ivar_build.rs, animation.rs, lib.rs
- Multi-line reference fixes required manual attention (replace_all only matches single lines)
- 1 unit test

## Key Decisions
- Skipped IvarController extraction (Phase 3) - coupling too tight, benefit too low vs effort
- Used `std::mem::take` for EventBus extraction from self before egui closures
- Kept gizmo hover as local variable (frame-local state, not cross-frame event)
- SceneManager fields all `pub` for direct access pattern consistency

## Learnings
- Multi-line `self.\n  .field` references require manual fixing after replace_all
- `private_interfaces` clippy lint catches pub structs with pub(crate) field types
- egui temp-data pattern was genuinely blocking testability - 23 string-keyed slots now have compile-time checking

### Code Review Fixes
- Ran vfx-code-reviewer on commit 2d65822
- Merged duplicate `SyncViewportToUsdCamera`/`SyncViewportCamera` into `SyncUsdCamera`
- Replaced stringly-typed `CameraProjectionChange(String)` with `CameraProjection` enum
- Changed `ExportEditLayer(String)` to `ExportEditLayer(PathBuf)`
- Fixed `EventBus::drain()` to preserve vec capacity (swap instead of take)

### Phases 5-7 Assessment
- Deferred: handle_node_graph_event (~600 lines) calls 10+ Renderer methods — can't decouple without trait-based interfaces
- GpuBackend and EguiAdapter have same deep coupling issue
- Phases 1-4 deliver ~80% of architectural value; remaining phases better tackled during Qt migration

## Result
- Renderer: ~50 fields -> ~25 fields
- 3 new subsystem structs with unit tests
- Zero egui imports in EventBus, SelectionManager, SceneManager
- Clean build, clean clippy, clean fmt
- Manual test: starts up and renders correctly

## Next Session
- Full manual test: pick objects, undo/redo, batch render, node graph
- Consider mutation methods on SceneManager to protect undo invariants
- Consider moving PrimSelected property-building into SelectionManager
