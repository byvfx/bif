# Development Log - 2026-03-05

## Session Duration

~30 minutes

## Goals

- Set up CI/CD pipeline for automated checks and releases

## What I Did

- Created `.github/workflows/ci.yml` with two jobs:
  - `check`: fmt, clippy, tests for bif_math/bif_renderer/bif_viewport on every push/PR
  - `release`: vcpkg (USD+OIIO) + OIDN SDK build, DLL bundling, GitHub release on v* tags
- Created `vcpkg.json` manifest for USD + OIIO dependencies
- Created `CHANGELOG.md` with Keep a Changelog format, `## [Unreleased]` section
- Updated `bif-commit.md` skill: added CHANGELOG update step, fixed co-author to Opus 4.6
- Updated `CLAUDE.md`: added CHANGELOG to pre-commit checklist
- CI release notes now extracted from CHANGELOG.md (was SESSION_HANDOFF.md)

## Learnings

- vcpkg binary caching via GitHub Actions cache should speed up repeat release builds
- Keep a Changelog format with `## [Unreleased]` section auto-populated during commits, renamed to version at tag time
- Skipping bif_core/bif_viewer in CI check job avoids USD/CMake dependency complexity

## Next Session

- Push to GitHub and verify `check` job passes
- Visual denoise test (pending from previous session)
- VFX code review on commit `71aae62`
- Ivar Xform transform fix

## Wiki Links

- [[bif-usd-integration|BIF USD Integration]] — USD pipeline improvements
- [[crate-structure|Crate Structure]] — crate refactoring
