# Development Log - 2026-03-03

## Session Duration

~1 hour

## Goals

- Install Intel OIDN prebuilt libraries
- Build with `--features oidn` and verify linking
- Run OIDN-gated unit tests
- Update setup_usd_env.ps1 for OIDN env vars

## What I Did

- Verified OIDN 2.4.1 prebuilt package at `D:/__projects/_programming/oidn-2.4.1.x64.windows/`
- Set `OIDN_DIR` env var + added `bin/` to PATH
- `cargo build --features oidn` — linked successfully (oidn-rs 2.3 vs OIDN 2.4.1, API-compatible)
- Hit `STATUS_DLL_NOT_FOUND` (0xc0000135) running tests — bash `export PATH` doesn't propagate to Windows DLL resolution
- Workaround: copied OIDN DLLs to `target/debug/` and `target/debug/deps/`
- All 69 bif_renderer tests pass including `test_denoise_beauty_only` and `test_denoise_with_albedo_normal`
- Updated `setup_usd_env.ps1`: added `$OidnRoot`, `OIDN_DIR` env var, OIDN bin/ to PATH
- Confirmed `cargo build --features oidn,oiio` works from PowerShell with updated setup script

## Learnings

- Windows DLL resolution: bash `export PATH` doesn't always propagate to child process DLL search
- Copying DLLs to target/debug/ works but is fragile (cargo clean wipes them)
- Proper fix: add OIDN bin/ to system PATH or use setup_usd_env.ps1
- oidn-rs 2.3 crate works with OIDN 2.4.1 prebuilt (minor version compatible)

## Next Session

- Visual test: render scene → click "Denoise (OIDN)" → verify noise reduction
- Visual test: batch render with denoise checkbox → verify denoised EXR
- Continue M29 validation (Houdini/usdview)
- Ivar: apply Xform transforms to baked mesh_data
