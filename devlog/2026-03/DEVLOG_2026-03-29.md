# Development Log - 2026-03-29

## Session Duration

~3 hours

## Goals

- Run /improve-architecture to identify architectural friction points
- Plan and begin implementing architecture deepening refactors
- Lay foundation for Linux cross-platform support

## What I Did

- **Architecture exploration:** Launched 3 parallel explore agents across 50K+ lines, 6 crates. Identified 6 deepening candidates (FFI bridge, renderer hub, node graph eval, scene loading, materials, textures).
- **USD workflow integration:** Read and incorporated BIF_USD_WORKFLOW.md — all refactors aligned with layer-aware editor direction (EditOperations, PayloadPolicy, layer isolation).
- **ARCHITECTURE_REFACTORS.md:** Created 5-phase plan with per-phase problem/design/files/tests/risks. Estimated 13-18 weeks, 45-65 new tests.
- **Phase 1 implemented:** Split cpp_bridge.rs (4,542 LOC) into:
  - `ffi_raw.rs` (898 lines) — all #[repr(C)] types + extern "C" block, pub(crate)
  - `ffi_convert.rs` (2,054 lines) — 17 conversion functions + 9 helpers + 44 tests
  - `cpp_bridge.rs` slimmed to 3,651 lines (-20%)
- **Cross-platform analysis:** Identified Windows-only build.rs issues (hardcoded CMake generator, vcpkg paths, CI), documented in ARCHITECTURE_REFACTORS.md Phase 2.

## Issues Found

- 9 pre-existing test failures in bif_core (asset files not found, export NullPointer) — not caused by our changes
- ffi_convert tests require USD DLLs in PATH even though they're pure Rust (bif_core test binary links against USD bridge)

## Current State

- Phase 1 complete, committed (7ae09e5)
- All 44 new tests pass, 149 total bif_core tests pass
- Build, clippy, fmt all clean
- UsdStage methods still have inline conversion (duplicated in ffi_convert) — delegation is next incremental step

## Learnings

- Rust allows `impl` blocks in separate files within the same crate — useful for decomposing God objects
- `#[repr(C)]` struct extraction is mechanically safe since types are just declarations
- Test binary linking means even pure-Rust tests in a crate with C++ FFI need the DLLs present at runtime

## Session 2: Phase 2 Cross-Platform (continued same day)

### What I Did

- **build.rs refactored:** Added 6 helper functions (`vcpkg_triplet()`, `vcpkg_fallback_paths()`, `cmake_build_subdir()`, `cmake_generator_args()`, `vcpkg_lib_bin_paths()`, updated `find_cmake()`). Replaced all hardcoded Windows paths with platform conditionals.
- **setup_usd_env.sh:** Created bash equivalent of PowerShell setup script for Linux/macOS (auto-detects triplet, sets LD_LIBRARY_PATH, PXR_PLUGINPATH_NAME).
- **main.rs:** Guarded `process::exit(0)` with `#[cfg(windows)]` — Linux/macOS clean up shared libs without deadlocking.
- **ci.yml:** Added `check-linux` job on ubuntu-latest running fmt, clippy, and tests for bif_math + bif_renderer.

### Verification

- Full workspace build: clean
- Clippy: clean (exit 0)
- Fmt: clean
- All tests pass on Windows (44 ffi_convert + math + renderer)

## Session 3: Phase 3 Node Graph Eval Engine (continued same day)

### What I Did

- **Explored node graph architecture:** Mapped all 4 files (2,065 lines), traced auto-compute flow from viewer.rs show_body() through NodeGraphEvent dispatch.
- **Created eval.rs** (726 lines): `collect_auto_compute_events()` pure function + `evaluate_node()` per-variant dispatch + `is_input_connected()` / `extract_scatter_params()` helpers + 19 tests.
- **Tests cover:** Primitive (auto-create, manual dirty, already-created), ScatterPoints (grid, surface needs input, surface with input, manual), PointInstancer (auto-compute, missing inputs, computing guard, failed guard, invalidate on disconnect, manual), UsdRead no-op, multi-node pass, already-computed skip.
- **Key insight from TODO (viewer.rs:151):** "nodes scrolled out of view won't cook" — eval.rs fixes this by evaluating ALL nodes regardless of UI visibility.

### Verification

- Build: clean, Clippy: clean, Fmt: clean
- 19/19 eval tests pass

## Session 4: Phase 4 Scene Pipeline (continued same day)

### What I Did

- **Explored scene_loader.rs** (2,350 LOC): Mapped all 9 methods, identified pure computation vs GPU upload boundaries. Key split points: L777 (compute ends) → L782 (GPU begins).
- **Created scene_pipeline.rs** (579 lines): 5 pure functions + 16 tests:
  - `resolve_prim_path()` — instance path resolution (moved from scene_loader.rs)
  - `build_material_index_lookup()` — material name → index HashMap
  - `expand_scene_instances()` — Scene → parallel arrays (transforms, mat IDs, proto IDs, paths) with hidden proto filtering, instancer append, max cap
  - `compute_world_bounds()` — prototype AABBs × instance transforms → world AABB
  - `apply_axis_correction()` + `apply_correction_to_transforms()` — USD stage metadata Z-up/unit correction

### Verification

- 16/16 tests pass, Clippy clean, Fmt clean

## Session Summary (Full Day)

| Phase | New Files | Tests | Lines |
|-------|-----------|-------|-------|
| 1: FFI Bridge Split | ffi_raw.rs, ffi_convert.rs | 44 | 2,952 |
| 2: Linux Cross-Platform | setup_usd_env.sh, build.rs, ci.yml, main.rs | 0 (CI) | 130 net |
| 3: Node Graph Eval | eval.rs | 19 | 726 |
| 4: Scene Pipeline | scene_pipeline.rs | 16 | 579 |
| 5: Renderer Decomp | node_dispatch.rs | 0 | 790 (moved 779) |
| **Total** | **8 new files** | **79** | **~5,177** |

All 5 phases of ARCHITECTURE_REFACTORS.md complete in one session.

## Session 5: Code Review + ffi_convert Wiring (continued same day)

### What I Did

- **Code review:** Ran Code Reviewer agent on all 5 phases. 2 HIGH, 3 MEDIUM, 2 LOW, 2 NIT findings.
- **Fixed all review findings:**
  - Deduplicated `resolve_prim_path` (scene_loader imports from scene_pipeline)
  - `panic!` → `unreachable!` in `extract_scatter_params`
  - `log::warn` on `canonicalize_for_usd` failure and AABB fallback
  - Documented `WorldBounds` vs `Aabb` distinction
  - TODO for `collect_export_context` move to ops.rs
  - Deleted stale "moved to" comment
- **Wired ffi_convert:** 17 UsdStage `get_*` methods now delegate to `ffi_convert::convert_*()`. Removed `#![allow(dead_code)]` from ffi_convert.rs. cpp_bridge.rs: 4,542 → 2,824 lines (38% reduction).

### Verification

- Build: clean, Clippy: clean, Fmt: clean, 44/44 ffi_convert tests pass

## Next Session

- Test build on Linux dev environment
- Wire eval.rs into main render loop (replace show_body auto-compute)
- Wire scene_pipeline.rs into reload_working_scene (replace inline compute)

## Wiki Links

- [[node-graph-system|Node Graph System]] — node graph pipeline
- [[bif-usd-integration|BIF USD Integration]] — USD integration
- [[point-instancer|Point Instancer]] — instancer rendering
- [[stage-layer-prim|Stage, Layer, Prim]] — USD stage management
