# Development Log - 2026-03-10

## Session Duration
~3 hours

## Goals
- Organize devlog directory into monthly subdirs
- Full codebase code review across all 6 crates
- Fix critical issues found

## What I Did

### Devlog Reorganization
- Moved 58 devlog files into `YYYY-MM/` subdirectories
- Updated `devlog/README.md` and `CLAUDE.md` path references

### Full Code Review (4 parallel VFX code review agents)
Reviewed all 6 crates: bif_math, bif_core, bif_renderer, bif_viewport, bif_viewer, bif_maketx.
Found ~50 issues across severity levels: 7 critical, 19 high, 20+ medium, several low.

### Critical Fixes Applied (5)
1. **Normal transform** — `instanced_geometry.rs` now uses inverse-transpose (correct for non-uniform scale)
2. **UsdEditLayer double-free** — `save()` nulls pointer before Drop runs
3. **ControlFlow::Poll → Wait** — was burning 100% CPU when idle, consolidated redraws in `about_to_wait`
4. **NEE MIS light PDF** — `sample_one` now includes 1/N light selection probability
5. **Dead code deletion** — removed `instanced_geometry_bvh.rs` (broken UB, never compiled)

### Quick Wins Applied (8)
1. Duplicate `gen_f32` in light.rs → import from material.rs
2. O(n²) mesh animation lookup → `enumerate()` in loader.rs
3. `#[inline]` on `Aabb::hit()` (BVH hot path, cross-crate inlining)
4. `DEFAULT_FAR_PLANE` 100 → 10000 (was clipping VFX scenes)
5. `axis_interval` catch-all `_` → explicit match + panic on invalid axis
6. Default BSDF/PDF mismatch → both return `1/(2*PI)` consistently
7. `.usd` binary file routing → C++ bridge (was going to pure-Rust USDA parser)
8. `CompositeProvider` dedup → `sort+dedup` vs O(n²) `Vec::contains`

### Result (commit 1)
- 10 files changed, +34/-319 lines (net -285)
- All tests pass (123: bif_math 41 + bif_renderer 82)

### Tier 2A — Rendering Correctness (4 fixes)
1. **Embree shading normals** — precompute per-instance inverse-transpose `Mat3`, apply via `inst_id` at hit time
2. **Viewport normals** — `combine_with_transforms` uses `Mat3::inverse().transpose()` instead of `transform * Vec4(n, 0)`
3. **Embree vertex stride** — 12→16 bytes (4 floats with padding for SIMD alignment)
4. **Lambertian scatter** — switched from `normal + random_unit_vector` to `cosine_weighted_hemisphere()` (Malley's method, no rejection sampling)

### Result (commit 2)
- 4 files changed, all 82 renderer tests pass

### Tier 2B — USD Safety (4 fixes)
1. **`UsdMesh::triangulate` bounds check** — break on truncated `face_vertex_indices` instead of panic
2. **Hardcoded USD toolkit path** — `validate.rs` now uses `USD_TOOLKIT_DIR` env var with fallback
3. **`source_dir: None`** — material loading now sets `source_dir` from input file path for relative texture resolution
4. **Silent USDA parse errors** — `unwrap_or(0.0)` → `unwrap_or_else` with `log::warn` on bad floats

## Remaining Fix Plan

### Tier 1 DONE
### Tier 2A DONE
### Tier 2B DONE
- Hardcoded paths → env vars
- `source_dir: None` → derive from file path
- Silent USDA parse errors → log warnings

### Tier 3 — Larger Refactors (~555 lines, 6-8 sessions)
- `ray_color` dedup (extract common bounce loop, -150 lines)
- EXR writer dedup (single generic function, -300 lines)
- `UsdStage` Send+Sync audit
- HDRI CDF flat Vec, BVH sort precompute, parallel Vec bundle
- Wildcard re-exports → explicit, two Ray types → unify

### Tier 4 — Deferred
- Renderer God object split, egui event bus, MAX_INSTANCES dynamic

## Learnings
- Inverse-transpose for normal transforms is critical for non-uniform scale — classic CG bug
- `ControlFlow::Poll` vs `Wait` in winit: Poll spins, Wait sleeps between events
- Light selection probability must be included in MIS PDF when sampling one of N lights
- `sort+dedup` is simpler than `HashSet` when you're already sorting

## Next Session
- Tier 3: ray_color dedup (-150 lines), EXR writer dedup (-300 lines)
- Remaining Tier 2 misc: RNG seed, ImageBuffer bounds, mesh dedup hash, camera sensitivity
