# Development Log - 2026-03-28

## Session 1: Phase 4 — External Targets + Comparison (~20 min)

### What I Did
- Implemented usdview subprocess target (Python script gen, USD_PYTHON/USD_TOOLKIT_DIR discovery, JSON output parsing)
- Implemented Houdini subprocess target (hython, measures both hou.usd.openStage and SOP usdimport cook time)
- Implemented comparison mode (load two YAML files, diff per-metric with delta_ms/delta_%/FASTER/SLOWER/~same)
- Added `compare` CLI subcommand, updated targets list to include usdview/houdini
- Added serde_json dependency for subprocess JSON parsing

---

## Session 2: Phase 5 — Historical Results + Download Helper (~15 min)

### What I Did
- Added `--save` flag to `run` command — auto-saves YAML to `benchmarks/results/{timestamp}_{target}.yaml` + `latest_{target}.yaml`
- Added `download` subcommand — lists missing official assets (Kitchen Set, ALab, Moore Lane) with download URLs and extract paths
- Added `download_url` field to SceneEntry registry

### All 5 phases of bif_perf now complete!

## Next Session
- Test usdview/houdini targets with real USD environment
- Run first real benchmarks on local scenes
- Consider adding bif_perf to MILESTONES.md as completed work
