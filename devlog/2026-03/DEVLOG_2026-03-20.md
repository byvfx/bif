# Development Log - 2026-03-20

## Session Duration
~2 hours

## Goals
- Implement 10-phase USD export gap closure plan
- Wire existing C++ bridge FFI into export_scene
- Remove legacy Rust USDA parser

## What I Did

### Phases Completed (7 of 10)
- **Phase 1:** Stage metadata export — set_stage_metadata + set_default_prim after layer creation
- **Phase 2:** Material export — export_materials() writes UsdPreviewSurface, bind_material per mesh
- **Phase 3:** Light + camera export — all 4 light types mapped to UsdLux, cameras with FOV-to-focal conversion
- **Phase 4:** Visibility export — hidden_prim_paths on ExportConfig, write_visibility per path
- **Phase 7:** Curves + points import — CurvesPrim/PointsPrim scene types, loader calls stage.curves()/points()
- **Phase 8:** DomeLight export — Light::Dome mapped to UsdLuxDomeLight with texture + rotation
- **Phase 10:** Remove Rust parser — deleted parser.rs (1229 lines), SceneBuilder, load_usda routes through C++ bridge

### Phases Partially Done (3 of 10)
- **Phase 5:** UsdPreviewSurface done; OpenPBR MaterialX needs C++ bridge extension
- **Phase 6:** GeomSubset export needs new write_geom_subset C++ FFI
- **Phase 9:** invisible_ids stored on PointCloud import; export needs C++ bridge extension

### Key Changes
- `export.rs`: +~350 lines — new functions (export_materials, export_lights, export_cameras, export_visibility, scene_light_to_usd), 4 new tests
- `scene.rs`: +35 lines — CurvesPrim, PointsPrim types + Scene fields
- `loader.rs`: -250 lines net — added curves/points loading, deleted SceneBuilder + parser dependency
- `parser.rs`: -1229 lines — deleted
- `point_cloud.rs`, `scatter.rs`: invisible_ids field on PointCloud

### Files Modified
- bif_core: export.rs, loader.rs, mod.rs, scene.rs, point_cloud.rs, scatter.rs
- bif_viewport: render.rs, lib.rs (ExportConfig new fields)
- Deleted: bif_core/src/usd/parser.rs

## Learnings
- ExportConfig without `..Default::default()` in test literals breaks when fields added — use default spread
- bif_core tests can't run without USD DLLs even for non-USD tests (binary links at load time)
- Quat::from_rotation_arc handles light direction → transform mapping cleanly

## Next Session
- Implement C++ bridge extensions: write_geom_subset, write_invisible_ids, MaterialX authoring
- Test roundtrip with pre-authored USD assets in usdview
- Move to M29.5 (egui upgrade) once export gaps fully closed
