# Development Log - 2026-03-22

## Session Duration
~4 hours (continuation from 2026-03-21 late session)

## Goals
- Fix VFX code review findings
- Deep dive on texture memory for large scenes
- Architecture review + make large scenes loadable

## What I Did

### Review findings (cc4b5ea)
- Fixed critical bug: normals lost on meshes without UVs (deferred copy was inside UV block)
- Fixed newNormals.reserve (was 0 no-op after deferred normals change)
- Improved PairHash with proper bit mixing (MSVC std::hash<int> is identity)
- Added sync_channel(32) backpressure on texture loading channel (was unbounded mpsc)
- Removed unnecessary .clone() in texture upload path (skip copy when no downscale)
- Raised UDIM atlas cap 16MP→32MP (better Ivar quality, still safe at 512MB)

### Texture scaling + memory (305451f)
- Raised MAX_VIEWPORT_TEXTURES 512→2048 for production scenes (1260+ textures)
- Adaptive texture downscale: 512px for 200+ textures, 1024px for 50+ (in background thread)
- Added usd_bridge_free_mesh_geometry() — frees normals/UVs/subdivision from C++ cache after Rust copy (~8GB savings on 335M vert scenes)
- Texture streaming progress logging (every 64 textures)

### Architecture review
- Software Architect + Code Reviewer graded codebase: A crate arch, B+ god object, C+ load perf (now fixed)
- Brainstormed large scene strategies: payload lazy loading, streaming geometry, bounding box mode, zero-copy FFI

### Testing
- Heavy scene (rt_base.usda: 335M verts, 6689 meshes, 2987 materials, 1260 textures)
- Scene loads without GPU crash — buffer guards + texture warnings working
- RAM still high (~128GB maxed) — C++ mesh cache free helps but USD stage + Rust copy + GPU buffers still large

## Current State
- Small/medium scenes work great (8.3x loading speedup, proper materials, textures)
- Heavy production scenes load without crashing but max RAM
- Texture streaming limited by GPU slot count (2048 now) and streaming rate

## Learnings
- Unbounded mpsc channels are hidden memory bombs — always use sync_channel for data-heavy messages
- Deferred optimizations must handle ALL code paths — the no-UV fallback was missed
- MSVC std::hash<int> is literally identity → custom hash mixing essential for sequential keys
- Production scenes (335M verts) need fundamentally different approach — data duplicated 3x (C++ → Rust → GPU)
- 128GB RAM not enough for naive eager loading of 335M vert scenes

## Next Steps
- Phase 2: bounding box mode for huge scenes (show hierarchy, load geometry on demand)
- Virtual texture atlas (M35+ milestone, packs 1000+ textures into atlas pages)
- Payload-based lazy loading via USD's payload mechanism
- M29.5: egui upgrade
