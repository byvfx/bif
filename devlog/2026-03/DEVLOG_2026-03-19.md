# Development Log - 2026-03-19

## Session Duration

~2 hours

## Goals

- Fix native instance material overrides (testballs.usda: 3 spheres all rendering with glass)
- Investigate rough metal rendering differences vs Karma

## What I Did

- **C++ bridge: Implicit geometry** — added `tessellate_sphere()` and `tessellate_cube()` in C++, dedup spheres by radius and cubes by size (not prim path), so same-radius spheres produce 1 mesh + N-1 native instances with `material_override_index`
- **C++ bridge: Lighting overhaul** — `get_light_attr()` helper for old/new USD schema compat, color temperature (Tanner Helland), leftHanded mesh orientation fix
- **Rust loader: Material override processing** — moved native instance loop after material binding, built bridge→scene material index lookup, clone-prototype pattern with `override_proto_cache` to avoid duplicates
- **Winding convention** — switched from CW to CCW throughout (primitives.rs, mesh.rs normals, C++ tessellation)
- **DomeLight** — auto-HDRI connection, Y-axis rotation extraction from transform
- **RectLight** — radiance-based emission (removed distance falloff), emit along local -Z
- **UI** — default sky gradient toggle checkbox, black background default

## Issues Found

- Original plan assumed C++ bridge was already producing native instances for implicits — it wasn't. Spheres with different prim paths weren't being deduped. Fixed by deduping by radius/size instead.
- Rough metal sphere renders differently from Karma — needs investigation (Ivar sample count, shading model, or property value differences)

## Learnings

- USD implicit geometry (UsdGeomSphere/Cube) needs explicit tessellation — no mesh data exists in the stage
- USD light attributes changed prefix between schema versions (bare name vs `inputs:` prefix)
- Rust-side mesh dedup by geometry hash can also cause material override issues for regular meshes (separate from implicit geometry) — noted for future fix

## Session 2 — Lighting Fixes

### What I Did

- **`treatAsPoint` support** — C++ bridge reads USD SphereLight `treatAsPoint` attr; when true, sets radius=0 so BIF uses point light path with correct 1/d² falloff (was using sphere cone sampling with broken R⁴/d⁴ falloff)
- **Delta light MIS fix** — added `is_delta` flag to `LightSample`; skip MIS power heuristic for delta lights (point/distant). MIS was crushing the specular peak because `bsdf_pdf >> light_pdf=1.0` at the highlight center, creating a ring/donut artifact
- **Shadow ray offset** — shadow ray origin now offset along surface normal (`rec.p + normal * 0.001`) to prevent self-intersection on curved geometry

### Learnings

- MIS power heuristic assumes both sampling strategies (light + BSDF) can reach the same directions. Delta lights violate this — only NEE can sample them, so MIS weight must be 1.0
- Sphere light cone sampling emission formula `intensity * sin²(θ_max)` causes R⁴/d⁴ falloff when combined with cone PDF — needs future fix for proper sphere lights

## Session 3 — Bound Material Inspector

### What I Did

- **Property inspector: Bound Material section** — added `bound_material: Option<Arc<Material>>` to `PrimProperties`, populated via prim_path → instance → prototype → material lookup on selection
- **Material rendering** — collapsible "Bound Material" section shows all OpenPBR params: base color swatch+RGB, metalness, roughness, specular weight, IOR, transmission, opacity, emission color/luminance (conditional), double-sided flag, texture paths (non-None only)
- User wants Houdini-style layout in next iteration

### Files Changed

- `crates/bif_viewport/src/property_inspector.rs` — field, builder, render functions
- `crates/bif_viewport/src/render.rs` — material lookup in PrimSelected handler

## Next Session

- Iterate on material inspector layout (Houdini-style)
- Run full test suite with USD env to verify no regressions

## Wiki Links

- [[openpbr-surface|OpenPBR Surface]] — OpenPBR material system
- [[shading-materials|Shading and Materials]] — USD shade handling
- [[geometry-schemas|Geometry Schemas]] — geometry processing
- [[bif-usd-integration|BIF USD Integration]] — USD integration
