# Development Log - 2026-03-25

## Session Duration

~1.5 hours

## Goals

- Run code reviews on M30 commits
- Fix review findings

## What I Did

### Code Reviews

- Ran VFX code reviewer + general code reviewer on 10 unpushed M30 commits
- 13 findings total: 3 critical, 7 important, 3 nits

### Review Fixes (13 items)

1. **SavePromptResult enum** — "Yes" now saves-then-proceeds (was silently cancelling = data loss)
2. **extract_project** reads live `eval_mode` (was hardcoded Auto)
3. **apply_project** restores `eval_mode` + clears `dirty_nodes`
4. **reset_project** clears scene/instances/usd_stage/selection (was leaving stale viewport)
5. **Cached recent_files** on Renderer (was `load_recent_files()` reading disk every frame at 60fps)
6. **FORMAT_VERSION** made `pub`, used in extract_project (was hardcoded `1`)
7. **mark_node_dirty** handles Cache nodes (cache invalidation was missing)
8. **RecentFiles::add** uses `std::fs::canonicalize` (was `to_path_buf` — no dedup on case/symlinks)
9. **apply_project** marks Primitive/Scatter/Instancer nodes dirty for Auto rebuild
10. **OnMouseRelease** labeled "(TODO)" in combo (unimplemented, was indistinguishable from Manual)
11. **Dirty flag** only set on mutating events (SelectNode/SetDisplayNode excluded)
12. **bincode fragility** documented in module doc (not forward-compatible)
13. **Title bar** cached + save_recent_files logs warnings + redundant scatter_graph_id removed

### Dielectric IOR Investigation

- Traced IOR from MaterialX → OpenPBR → path tracer refraction code
- Renderer supports transmission/refraction (Snell's law + Schlick Fresnel in scatter_transmission)
- Glass beaker shows no refraction because: 0/2 meshes bound to materials → default `transmission_weight=0.0`
- Not a code bug — the USD file has no material binding. Refraction pipeline is functional.

---

## Session 2 Duration

~2 hours

## Goals (Session 2)

- Implement M31: Per-node scene graph visualization

## What I Did (Session 2)

### M31: Per-Node Scene Graph Visualization (4 phases)

**Phase 1: Source node tagging**

- Added `source_node: Option<GraphNodeId>` to `ProceduralPrim`
- `build_scene_graph_cache()` now accepts `node_proto_map` + `node_cloud_map`, builds reverse maps to tag each prim with its origin node
- Added `CachedSceneGraph::prim_count_by_node()` helper

**Phase 2: Prim count badges**

- Added `node_prim_counts` to `NodeGraphContext`, computed after cache rebuild
- `render_node_graph()` passes counts to `SceneNodeViewer`
- Node headers show `[N]` badge via `ui.colored_label(theme::TEXT_DISABLED, ...)`

**Phase 3: Node scene browser mode**

- `SceneBrowserViewMode` enum: FullScene | NodeContribution(GraphNodeId)
- `NodeFilteredProvider` wraps CompositeProvider, precomputes allowed paths from upstream node set
- Tab bar (Scene/Node) in left panel, auto-updates on node selection change
- Made `collect_upstream_nodes` pub for cross-module use

**Phase 4: Row highlighting**

- `PrimDisplayInfo.source_node` propagated from `CompositeProvider::get_prim_info()`
- `render_prim_row` accepts `highlight_node`, tints matching rows green

### Tests

- 4 new tests: source tagging, prim count by node, filtered provider, (existing tests updated)
- 112 pass, 2 pre-existing failures (flaky timing test + Material::default row count)

### Files Modified

- `scene_browser.rs` — ProceduralPrim, CachedSceneGraph, NodeFilteredProvider, SceneBrowserViewMode, rendering
- `render.rs` — cache build call, prim counts, tab bar, provider switching, highlight wiring
- `node_graph/mod.rs` — render_node_graph signature
- `node_graph/viewer.rs` — SceneNodeViewer prim_counts, badge display
- `node_graph/ops.rs` — collect_upstream_nodes pub visibility
- `lib.rs` — NodeGraphContext field, pub exports

## Learnings

- Tag+filter approach (no re-evaluation) is simple and performant for node visualization
- Reverse maps from existing node_proto_map/node_cloud_map avoid new tracking infrastructure
- Pre-existing test_build_property_rows_with_material failure needs investigation (Material::default row count)

## Next Session

- Push commits to origin
- Manual test M31 with Primitive → Scatter → Instancer chain
- Investigate pre-existing test_build_property_rows_with_material failure
- Start M32: USD Composition Inspector & Opinion Trace
