# Development Log - 2026-03-30

## Session Duration
~1.5 hours

## Goals
- Debug why Pixar's PointInstancedMedCity.usd renders no instances in BIF

## What I Did

### Investigation
- Inspected PointInstancedMedCity.usd via Python/pxr: 40K instances, 8 Xform prototypes each wrapping a Mesh child
- Discovered all instancer attrs (positions, orientations, scales, protoIndices) authored ONLY at time sample t=184.0 with no default values
- `ComputeInstanceTransformsAtTime(Default)` returns 0 transforms; `ComputeInstanceTransformsAtTime(0)` returns 40K

### Bug 1: Time-sampled data read at Default time (C++ bridge)
- `cpp/usd_bridge/usd_bridge.cpp` — instancer caching used `UsdTimeCode::Default()` for all reads
- Fix: added `evalTime` fallback that checks time samples on positions/orientations/scales, uses stage `startTimeCode` or first sample
- Applied to: GetProtoIndicesAttr, ComputeInstanceTransformsAtTime, velocities, angular velocities

### Bug 2: Xform vs Mesh prototype path mismatch (Rust loader)
- Instancer targets: `/Prototypes/prototype_0` (Xform), but prototype_map keys: `/Prototypes/prototype_0/mesh_0` (Mesh)
- Fix: after inserting mesh path into prototype_map, also insert parent Xform path via `entry().or_insert()`
- File: `crates/bif_core/src/usd/loader.rs`

### Test asset
- Added `assets/PointInstancedMedCity.usd` from Pixar for testing

## Learnings
- USD PointInstancer data can be entirely time-sampled with no default values — must handle this case
- Common USD pattern: PointInstancer prototype targets are Xforms wrapping child Meshes, not Meshes directly
- `ComputeInstanceTransformsAtTime` interpolates/holds values for any numeric time code, even outside the authored sample range

---

## Session 2 Duration
~1 hour

## Session 2 Goals
- Set up GitHub Pages site with dev diary and help/manual

## Session 2 — What I Did

### GitHub Pages with mdBook
- Evaluated 3 options (mdBook, Zola, MkDocs Material) — chose mdBook for zero-friction Rust-native workflow
- Created `site/book.toml` — mdBook config with navy theme, search, repo link
- Created `site/theme/favicon.svg` — "B" letter favicon (dark bg, bold letter)
- Created `site/src/introduction.md` — landing page adapted from README
- Created `site/src/reference/getting-started.md` — build/install/run guide from README
- Created `site/src/reference/architecture.md` — crate structure from MILESTONES
- Created `scripts/generate-site.sh` — core automation:
  - Copies devlog/ entries preserving month dirs
  - Copies docs/usd/ reference docs
  - Copies CHANGELOG.md
  - Auto-generates SUMMARY.md with month-grouped devlog (newest first), titles extracted from headings
- Created `.github/workflows/pages.yml` — CI deploys on push to main
- Updated `.gitignore` for generated content (site/book/, site/src/devlog/, etc.)
- Built locally: 104 nav items, 85 devlog files, 10 USD docs, all 5 months

### Key Decision: mdBook over Zola/MkDocs
- Zero workflow change: write devlog md, push, done (no frontmatter needed)
- Rust-native single binary, ~1s build
- Auto-generated SUMMARY.md eliminates maintenance burden
- Devlog is a journal not a blog — tree nav > pagination

## Session 2 Learnings
- mdBook v0.5.2 picks up `theme/favicon.svg` automatically
- mdBook SUMMARY.md can use `()` as href for non-link section headers
- Shell script to auto-generate SUMMARY.md from file tree is ~60 lines, solves the main mdBook weakness for blog-style content

## Next Session
- Enable GitHub Pages source = "GitHub Actions" in repo settings
- Verify deployment works on push
- Run vfx-code-reviewer on commit
- Test with other PointInstancer files for regression
- Continue v0.13.0 work
