# Development Log - 2026-03-17

## Session Duration
~1 hour

## Goals
- Fix UDIM texture loading regression for UNC network paths (v2 — after v1 broke viewport textures)
- Commit prior uncommitted work from last session

## What I Did
- Analyzed v1 regression: normalizing paths in `load()`/`load_linear()`/`resolve_texture_path()` broke `index_map` lookups because `MaterialGpu::from_material()` uses forward-slash keys
- Reverted normalization from cache-key paths (load, load_linear, resolve_texture_path)
- Kept normalization at filesystem boundaries only (find_udim_tiles, resolve_path)
- Added `starts_with("//")` UNC detection in viewport's `resolve_texture_path()` — `Path::is_absolute()` returns false for forward-slash UNC on Windows
- Split uncommitted changes into 4 logical commits:
  1. USD material export: C++ bridge + Rust FFI + scene material GC
  2. Ivar material cache invalidation + prewarm + viewport integration
  3. Fix UDIM texture loading for UNC network paths
  4. Cleanup: remove debug logs, add code review docs

## Key Decision
**Rule:** `index_map` keys and cache keys must stay in original format (forward slashes from USD). Normalize only at filesystem boundaries.

## Verification
- `cargo build` — clean
- `cargo clippy -- -D warnings` — clean
- `cargo fmt --check` — clean
- `cargo test -p bif_math -p bif_renderer` — 85 tests pass
- bif_viewport tests: DLL_NOT_FOUND (pre-existing env issue, not related)

## Learnings
- Windows `Path::is_absolute()` returns false for `//server/share` (forward-slash UNC) — must check `starts_with("//")` explicitly
- Normalizing paths at API boundaries instead of filesystem boundaries causes cache key mismatches — subtle and hard to debug

## Next Session
- Manual test: load local USD with textures (regression check)
- Manual test: load BellA.usd with UDIM textures over UNC path
- M29.5 egui upgrade or M30 persistence
