# Development Log - 2026-03-16

## Session Duration
~3 hours

## Goals
- Implement architecture cleanup plan: Renderer decomposition + quick wins

## What I Did

### Phase 1: UsdStage Send+Sync — SKIPPED
- Deep dive confirmed `Arc<UsdStage>` is shared across rayon workers in batch_render.rs
- `get_mesh_vertices_at_time()` called from worker threads via FFI
- Safety design (thread-local buffers, immediate copy, const FFI) is sound
- Prior audit (2026-03-10) already validated — removing impls would break build

### Phase 2: Renderer Sub-Struct Extraction
Extracted 4 sub-structs from Renderer (30 fields moved, ~465 access sites updated):

| Sub-struct | Fields | Heaviest file | Access sites |
|-----------|--------|---------------|-------------|
| GpuContext | 4 (surface, device, queue, config) | scene_loader.rs | 73 |
| CameraState | 7 (camera, uniform, buffer, bind_group, source, lock, usd_camera) | lib.rs | 87 |
| IvarContext | 8 (state, texture, view, sampler, bind_group, layout, pipeline, materials) | ivar_build.rs | 240 |
| NodeGraphContext | 11 (graph state, node maps, instancer results, caches) | render.rs | 65 |

Approach: define sub-struct, replace fields in Renderer, update constructor, bulk `replace_all` per file, fix multi-line patterns.

### Phase 3: Quick Wins
- Removed `pub use bif_math::{Ray, Vec3, Aabb, Interval}` from bif_renderer (changed to `pub(crate)` for Ray/Vec3, removed Aabb/Interval entirely)
- Fixed examples (cache_bench, simple_render) to import Vec3 from bif_math
- Converted DenoiseError to `#[derive(thiserror::Error)]`

## Issues Found
- Multi-line method chains (`self\n.device`) not caught by single-line `replace_all` — needed separate fixes
- bif_viewer accessed `renderer.camera` across crate boundary — CameraState needed `pub` visibility
- Examples imported Vec3 from bif_renderer re-export — needed updating

## Learnings
- Bulk `replace_all` is effective for mechanical refactoring but misses multi-line patterns
- Always check downstream crates (bif_viewer) and examples for field visibility
- `pub(crate)` is the right middle ground for internal-only re-exports

### Review Feedback Fixes
- Tightened CameraState field visibility: `camera_uniform`, `camera_buffer`, `camera_bind_group`, `viewport_camera_source`, `camera_locked`, `selected_usd_camera` → `pub(crate)` (only `camera` stays `pub` for bif_viewer access)
- Extracted `IvarState::invalidate_scene()` — replaces 4 identical 4-line reset blocks in scene_loader + base of invalidate_ivar_cache
- Extracted `IvarState::reset_on_resize()` — replaces 12-line block in lib.rs resize handler
- Net -28 lines (39 added, 67 removed)

## Current State
- Renderer: 69 direct fields + 6 sub-structs (GpuContext, CameraState, IvarContext, NodeGraphContext, AsyncChannels, UiLayout, SceneInstances)
- All checks pass: build, clippy, fmt, 85 renderer tests
- Two commits: `68db5ec` (decomposition), `99d715c` (review fixes)

## Next Session
- M29.5 egui upgrade
- Consider further Renderer decomposition: MaterialContext (~7), TextureContext (~4), EguiContext (~3)
- UsdStage Send+Sync confirmed sound — no action needed
