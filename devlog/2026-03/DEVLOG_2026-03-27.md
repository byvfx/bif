# Development Log - 2026-03-27

## Session Duration
~2 hours

## Goals
- Design and implement Phase 1 of a modular USD performance metrics system

## What I Did
- Fetched and analyzed USD `ref_performance_metrics.html` and `maxperf.html` docs
- Designed architecture: Metric trait, Target trait, AuditCheck trait, scene registry, report system
- Created new `bif_perf` crate in `benchmarks/` (16 files)
- Implemented 4 metrics: StageOpen, PayloadLoad, StageClose, FullLoad
- Built runner with configurable iterations + warmup + stats (min/max/mean/median/stddev/p95)
- Scene registry with tier-based filtering (simple/medium/large/official)
- Terminal table (tabled), YAML, CSV report output
- CLI with `run` and `list` subcommands via clap
- All checks pass: build clean, clippy -D warnings clean, fmt clean, 5/5 tests pass
- Fixed pre-existing fmt issues in bif_core/texture.rs and bif_viewport/texture_loader.rs

## Key Decisions
- New crate rather than inside bif_core — keeps CLI/subprocess deps separate
- Trait-based metrics (object-safe `dyn Metric`) — each metric is one file, composable
- No thread safety needed — benchmarking is inherently sequential for accuracy
- Scene registry is declarative data, not config file — simple, type-safe
- Stats module uses pure Rust math — no external stats crate

## Learnings
- USD's official methodology: 100 iterations, warm FS cache, min/max/mean
- `usdmeasureperformance.py` supports custom metrics via `testUsdviewInputFunction()`
- maxperf.html key tips: jemalloc (2x), binary .usdc, payloads, minimize prims, monitor layers
- clippy now enforces `is_none_or()` over `map_or(true, ...)` and `is_multiple_of()` over `% == 0`

## Next Session
- Phase 2: MeshExtract, MaterialLoad, PrimTraversal metrics + YAML/CSV output
- Phase 3: AuditCheck trait + best-practice checks from maxperf.html
- Phase 4: usdview/Houdini subprocess targets + comparison mode
- Phase 5: historical results storage, auto-download for official assets
