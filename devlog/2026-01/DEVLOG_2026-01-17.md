# Development Log - 2026-01-17

## Session Goal

Viewport GPU textures + per-instance materials (M17 groundwork)

## Completed

- Added GPU texture upload + array binding for viewport (128 slots)
- Introduced per-instance `material_id` and material table buffer for lookups
- Wired texture sampling in viewport shader (diffuse only)
- Added EXR/HDR linear-path decoding in texture loader
- Fixed Vulkan device limits and enabled array indexing features for texture arrays
- Verified viewer launch + USD load without crashes (Vulkan warnings remain external)

## Commits This Session

```text
a5a0daa Fix Vulkan texture array device limits
9a94b5d Fix texture loader and scene reload bindings
cbdbcf0 M17: sample textures in viewport shader
d87796e M17: add per-instance material IDs
7094c3f M17: add material table and texture bind group
93f867f M17: add GPU texture upload scaffolding
```

## Key Files Modified

| File | Changes |
|------|---------|
| crates/bif_core/src/texture.rs | EXR/HDR linear decode path + extension detection |
| crates/bif_viewport/src/lib.rs | GPU texture array upload, material table, per-instance IDs, device limits |
| crates/bif_viewport/src/shaders/basic.wgsl | Texture array binding + diffuse sampling |

## Learnings

- wgpu requires `TEXTURE_BINDING_ARRAY` and a higher `max_sampled_textures_per_shader_stage` for texture arrays
- Non-uniform indexing still works without explicit WGSL helper functions on this stack

## Tests

- cargo build
- Ran viewer with USD load (Vulkan warnings from external layers)
