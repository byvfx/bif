# Development Log - 2026-06-09

## Session Duration
~2 hours

## Goals
- Issue #6 Phase 2: add `SceneCmd` + `Renderer::execute()`, route the `node_dispatch.rs` bookkeeping arms through one mutation site.

## What I Did
- **Brainstormed + planned Phase 2** → `docs/agent-plans/2026-06-08-issue6-phase2-scenecmd.md`. Key design calls:
  - **Coupling-scoped, not full routing.** `SceneCmd` wraps only the desync-prone `node_outputs ↔ working_scene` proto/cloud bookkeeping + preview upload. `reload_working_scene`/`load_primitive`/`load_usd_scene`/`export_scene`/`compact_materials` stay direct (coarse, already factored; wrapping them = `Result` plumbing for zero safety gain).
  - **No `Custom` escape hatch.** Reading the 4 "complex" arms showed the tangled parts are computation/loaders, not mutations. `ExportUsd` does zero working-scene mutation. Every routed mutation decomposes into a named verb.
  - **`execute() -> ()` (infallible), per-command inline.** Arms needing a return value (`load_primitive` → `proto_id`) call the method directly, then emit `SceneCmd`s with computed data.
- **Task 1** (`scene_cmd.rs`): 6-verb `SceneCmd` enum + `Renderer::execute()`. Implementer boxed `AddCloud.cloud` (`Box<PointCloud>`) to satisfy clippy `large_enum_variant` — accepted (good for phase 3's `Vec<SceneCmd>`); updated the plan's Task 2 call site to `Box::new(cloud)`.
- **Task 2** (`node_dispatch.rs`): routed 7 arms through `self.execute(...)`. Net −74 lines (all deleted inline coupling). Removed the temporary `#[allow(dead_code)]` once callers existed.
- **Verified independently** (didn't trust subagent reports): reviewed the full diff vs original, ran build + clippy + fmt + the 181-test suite myself. Grep guard: zero `node_outputs.entry`/`add_point_cloud`/`remove_point_cloud` left in `node_dispatch.rs`.
- Executed via subagent-driven-development (fresh sonnet implementer per task, controller-side verification).

## Key Decisions & Why
- Pushed back on my own earlier "all 21 arms / never mutate directly" pick after reading the code — full routing would wrap coarse loaders + thread `Result` for no benefit. Settled on the tighter coupling-scoped surface. 7 arms routed; 14 out-of-scope/no-op.
- `DeleteNode` restructure: snapshot `had_cloud`/`had_protos` before draining, then `RemoveNodeCloud` (takes cloud, keeps proto entry) + `RemoveNodeProtos` (drains rest) → same end state as the original single `remove()` + snapshot. Verified reverse-order reindex preserved.

## Learnings
- IDE/LSP `new-diagnostics` can be stale mid-edit — saw a phantom `E0308` (Box mismatch) that a real `cargo build` proved already fixed. Always confirm against an actual build.
- dead_code on an inherent `pub(crate)` method with no caller yet is real; an `#[allow]` is the honest temporary bridge between a "define" task and a "wire callers" task — removed in the same PR.
- The only behavior delta in a clean command-extraction refactor is often just log-message detail (lost `cloud_id`/`proto_ids` in two `log::info!` lines) — flag it, it's inert.

## Next Session
- Open the PR, run `/vfx-code-reviewer` on the diff, squash-merge.
- **Phase 3:** `node_graph/behavior.rs` — move per-node `evaluate`/`register_prims`/`apply` (apply *constructs* `SceneCmd`s, making the CPU verbs unit-testable via a free function — see plan's "Testing & Future Work" / penciled test-`Renderer`).

---

## Phase 3a addendum — `SceneNode::evaluate` extraction

- **Decomposed Phase 3 into 3a/3b/3c** (evaluate / register_prims / apply); did 3a. Plan: `docs/agent-plans/2026-06-09-issue6-phase3a-evaluate.md`. Branch `refactor/issue6-phase3a-evaluate` (stacked on the phase-2 branch — docs would otherwise conflict with PR #9; code files don't overlap).
- **What:** `eval.rs::evaluate_node`'s per-variant `match` → `SceneNode::evaluate(&mut self, id, &EvalCtx, EvalMode) -> EvalOutcome` in new `node_graph/behavior.rs`. `evaluate_node` now a generic delegator.
- **Key design — `EvalCtx` as precomputed data, not a snarl borrow.** `evaluate(&mut self)` borrows the node out of the snarl, so it can't also hold `&snarl` for connection queries → the delegator reads connections first into `EvalCtx`. Payoff: `evaluate` is pure of the graph and unit-testable with no Snarl/GPU/USD (6 new pure tests). Chose `&mut self` (node flips own flags) over `&self`+status-descriptor to keep ONE per-variant match.
- **`extract_scatter_params` inlined + deleted** (its fields are bound in the ScatterPoints arm).
- **Verified:** build 0, clippy 0, **187 bif_viewport tests** (181 + 6). Diff reviewed vs original — 1:1 behavior. The `scatter_surface_needs_input` test passing confirms `scatter_points()` defaults to `Surface` (non-vacuous).
- **Process note:** the implementer subagent stalled (its Bash tool can't source the PowerShell USD env to run tests); I ran the USD-env suite + committed. Watch for this on future subagent dispatches — tell them to skip the USD-env test run and rely on me for it.
- **Next:** 3b `register_prims` (scene_browser per-variant prim registration → `behavior.rs` + `ProcPrimSink`).
