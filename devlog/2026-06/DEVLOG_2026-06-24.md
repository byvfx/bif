# Development Log - 2026-06-24

## Session Duration
~2 hrs (PR wrap-up)

## Goals
- Finish audit/node-graph-22 + open PR

## What I Did
- Verified all 5 plan tasks shipped (get_node_info, setters, connect_pins, NodeParamPanel, wire drag-connect)
- Updated CHANGELOG [Unreleased]
- Opened PR for audit/node-graph-22

## What I Also Did (continued session 2026-06-25)
- Fixed all 8 vfx-code-reviewer findings (commit a3d2541): project.mark_dirty() on all param setters + connect_pins, flush_node_graph() + bump_scene_browser_revision() in on_node_graph_connect_pins bridge, stale-wire cleanup in C++, json_str via serde_json, Vec collect → any()
- Addressed Copilot comment re: TestHarness re-implementing mutations — extracted 5 private snarl_* helpers shared by Renderer methods and TestHarness (commit e6c3bc1); added 4 dirty-flag tests; 198 tests green

## Learnings
- cxx-qt bridge pattern: declare in `extern "RustQt"`, impl with `Pin<&mut Self>`; C++ calls `m_state->on_*()` directly
- `scene_graph_dirty` must be set on structural graph mutations (connect_pins) to trigger re-evaluation
- Need both `setup_usd_env.ps1` and `setup_qt_env.ps1` sourced before `cargo clippy` (bif_qt requires Qt)
- Headless test harness pattern: extract snarl mutation logic to module-level helpers; both Renderer methods and TestHarness delegate to them; TestHarness tracks dirty flags manually instead of calling Renderer (which needs GPU)

## Next Session
- Merge PR #23 after review passes
- v0.17.0 work (issues #13–#15, Viewport perf)
