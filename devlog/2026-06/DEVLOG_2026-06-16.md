# Development Log - 2026-06-16

## Session Duration
~1.5 hours (spread; planning + GitHub PM setup)

## Goals
- Review a drafted GitHub project-management setup plan
- Stand up GitHub as roadmap source of truth (milestones/labels/issues/board)
- Slim local roadmap docs to match

## What I Did

**Plan review** — drafted plan had ~5 executability errors vs live state:
- Auth token missing `project` scope (every `gh project` call would fail)
- Labels already existed + hex collisions (`bug`/`documentation` dupes)
- `gh milestone list` isn't a real command (verification step would fail)
- `gh project` can't create Views/timeline — manual web step; right-sized for solo
- Milestone→theme mapping was a deliberate re-scope (confirmed), not an error

**Decisions:** re-scope intentional (v0.17.0=Viewport perf); AI→`backlog` label; #5 PathTracer→v0.21.0; keep default labels; current release = v0.16.9; solo-lean Board (no date/Priority fields).

**GitHub (scripted via gh):**
- 6 labels: `refactor/rfc/perf/infra/blocked/backlog` (non-colliding hexes)
- 6 milestones v0.17.0–v0.22.0 (via REST; no "Backlog" milestone)
- #5 → v0.21.0 + `refactor`; #13–15 granular v0.17.0 (acceptance criteria); #16–20 placeholders
- All 9 open issues on "BIF Roadmap" Project (Board template, created manually)

**Docs:**
- `CLAUDE.md` — killed stale roadmap line (AI-at-v0.18.0, closed-#6), → GitHub pointer
- `MILESTONES.md` — added v0.16.9 row, replaced stale Next-Releases table w/ pointer, fixed In-Progress line
- `ROADMAP_DETAIL.md` — preserved future design detail under "Future Design Notes" (Context System deferred, AI backlog), flagged pre-rescope

## Learnings
- `gh` has no `milestone` subcommand — use `gh api repos/<o>/<r>/milestones`.
- `gh project` needs `project,read:project` scope (separate from `repo`) + can't author views (web only).
- Solo PM: milestones = the ordered roadmap; a single Board view is enough — date/Priority fields are team machinery.
- CLAUDE.md project-context had drifted from MILESTONES.md and seeded the plan's errors — keep the high-level roadmap in ONE authoritative place (now GitHub).

## Next Session
- Start v0.17.0: pick #13/#14/#15 from the board → In Progress
- Likely first: #13 cpp_bridge.rs split (refactor, unblocks the rest)
- SESSION_HANDOFF.md narrowing still deferred (per decision)
