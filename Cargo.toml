[workspace]
resolver = "2"
members = [
    "crates/bif_math",
    "crates/bif_core",
    "crates/bif_renderer",
    "crates/bif_viewport",
    "crates/bif_viewer",
    "crates/bif_maketx",
    "crates/bif_qt",
    "benchmarks",
]

[workspace.package]
version = "0.14.0"
edition = "2021"
authors = ["Brandon Young"]
license = "MIT"
repository = "https://github.com/byvfx/bif"

[workspace.dependencies]
# Math and utilities
glam = "0.29"
bytemuck = { version = "1.14", features = ["derive"] }

# Parallel processing
rayon = "1.10"

# Rendering
wgpu = "22.1"
pollster = "0.3"
raw-window-handle = "0.6"

# Qt bindings (v0.15 migration — see ADR-006)
cxx = "1.0"
cxx-qt = "0.7"
cxx-qt-lib = { version = "0.7", features = ["qt_full"] }
cxx-qt-build = "0.7"
qt-build-utils = "0.7"

# UI - egui 0.29 (stable, works with wgpu 22)
egui = "0.29"
egui-wgpu = "0.29"
egui-winit = "0.29"
winit = "0.30"

# Scene/serialization (future USD work)
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Error handling
anyhow = "1.0"
thiserror = "1.0"

# Logging
env_logger = "0.11"
log = "0.4"

[profile.dev]
opt-level = 1  # Basic optimizations for faster iteration

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
