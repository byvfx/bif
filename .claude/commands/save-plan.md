---
allowed-tools: Read, Edit, Write, Glob
description: Save an architecture or tooling plan into the repo with the correct path and filename
---

Follow the canonical workflow in `agents/workflows/write-plan.md`.

When saving:

- Save to `docs/agent-plans/YYYY-MM-DD-<slug>.md`.
- Update an existing canonical file instead of creating duplicates when appropriate.

Plans are free-form prose — options, decision, rationale, open questions. For executor-ready briefs, use `/save-handoff` instead.
