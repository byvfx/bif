---
allowed-tools: Read, Edit, Write, Glob
description: Save an execution handoff into the repo with the correct path and filename
---

Follow the canonical workflow in `agents/workflows/write-handoff.md`.

When saving:

- Save to `docs/agent-handoffs/YYYY-MM-DD-<slug>.md`.
- Structure the content using the required sections from `agents/base/HANDOFF_CONTRACT.md`.
- Update an existing canonical file instead of creating duplicates when appropriate.

For architecture or tooling plans that are not executor-ready, use `/save-plan` instead.
